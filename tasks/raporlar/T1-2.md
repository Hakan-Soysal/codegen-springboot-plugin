# T1.2 Raporu — Manifest/Contract POJO'ları + Jackson yapılandırması

**Durum:** DONE · **Commit:** `8e58e1e` (yalnız gen-core, push yok) · **Tarih:** 2026-07-03

## Yapılanlar

### Step 5.1 — `techgen.core.Json`
`gen-core/src/main/java/techgen/core/Json.java`:
- `JsonMapper.builder()` ile: `FAIL_ON_UNKNOWN_PROPERTIES=false`, `ACCEPT_CASE_INSENSITIVE_PROPERTIES=true`.
- ExprNode (de)serializer'ları `SimpleModule` olarak kayıtlı. T1.1 verifier notu dikkate alındı:
  ExprNode tip-seviyesi `@JsonDeserialize/@JsonSerialize` anotasyonları AYNI sınıfları işaret
  ediyor — modül kaydı çakışan ikinci mekanizma değil, aynı davranış; testle doğrulandı
  (ExprNodeTest 10 test test-lokal mapper'la, ModelParseTest merkezî mapper'la yeşil).
- `parse(String, Class<T>)`: `"null"` girdisi → `JsonMappingException("null deserialize: ...")`.
- camelCase Jackson default'u; snake_case/kebab stratejisi AÇILMADI (anti-pattern §8).

### Step 5.2 — Manifest modeli (35 record, `techgen.core.model`)
Sözleşme §1 / Manifest.cs birebir alan listesiyle: `ManifestJson` (mode, contract, meta,
deployables, modules, operations, entities, types, errors, events, subscriptions, externals,
uncharted, callEdges, coverage), `Meta`, `Deployable`, `ModuleDecl`, `ParamJson`, `SignatureJson`,
`ServingArg`, `ServingJson`, `AccessJson` (4 liste), `GuardedExpr(text, ast:ExprNode, guardRef)`,
`Consistency`, `Abac(permit:ExprNode)`, `Idempotent`, `PaginationKey`, `Pagination`,
`ExtJson(ns, name, args:Map<String,JsonNode>)`, `OperationJson` (21 alan sözleşme sırasıyla),
`SourceOfTruth`, `EntityFieldJson` (targetModule/crossModule dahil — fixture'da yok ama şemada),
`EntityJson`, `FieldJson`, `TypeJson`, `ErrorJson`, `EventJson`, `EventRef`, `ConsumerRef`,
`SubscriptionJson`, `CallTarget`, `CallEdgeJson`, `BoundaryOpJson`, `ExternalJson`,
`UnchartedEntity`, `UnchartedType`, `UnchartedJson`, `Coverage`.
- Sözleşmede NON-nullable liste alanları compact ctor'da `Objects.requireNonNullElse(x, List.of())`.
- Sözleşmede nullable olanlar (`ext`, `TypeJson.fields/values`, `BoundaryOpJson.serving/validation`,
  `ServingArg.params` vb.) referans tipi + `/* @Nullable */` yorumu ile null kalır (normalize edilmez).

### Step 5.3 — Contract modeli (13 record)
`ContractFile(meta, operations, entities, actors, relations:List<JsonNode>, processes, flows)` —
tüm alanlar nullable ve KASITLI normalize edilmez (TestPlanBuilder'ın `processes==null||flows==null`
→ boş-TestPlan kısa devresi null/boş ayrımına dayanır; testle sabitlendi). `ContractMeta`,
`ContractSignature`, `ContractGuard`, `ContractEffect(kind, target:JsonNode, expr:ExprNode, text)`
(target toleranslı raw — string VEYA array), `ContractAccess(writes, reads)` (2 liste),
`ContractOp`, `ContractEntity`, `ContractActor`, `ProcessJson`, `ProcessStage`, `FlowJson`, `FlowStep`.

### Step 5.4 — Testler
`ModelParseTest.java` — 13 test: fixture manifest sayımları (operations=4, entities=2, types=4,
errors=1, events=1, subscriptions=1, externals=1, uncharted=1, callEdges=2, mode=="linked");
CreateInvoice detayları (creates=["Invoice"], throws=["DuplicateInvoice"],
idempotent.keys=["customerId"], ext=2, validation[0].ast BinaryNode); AccessJson 4-liste;
contract biz.CreateInvoice writes=["biz.Invoice"] 2-anahtar; ContractEffect.target string + array
(iki mini-JSON regression); studyo manifest 43 op + studyo contract exception'sız; bilinmeyen-alan
toleransı; `Json.parse("null", ...)` → exception (6.3); eksik-liste normalize; case-insensitive;
ContractFile nullable bölümlerin null kalması.

## Acceptance sonuçları (gerçek koşum)
- 6.1: `mvn -q -pl gen-core test` → **exit 0** (gözlemlendi). Toplam 24 test
  (Placeholder 1 + ExprNode 10 + ModelParse 13), 0 failure, 0 error.
- 6.2: sayım assert'leri surefire çıktısında yeşil (`manifestFixture_topLevelCounts` vb.).
- 6.3: `nullDeserializeThrows` yeşil.

## Notlar / sapmalar
1. **Task metnindeki "16 alan" sayısı**: Task §5.2 "ManifestJson (16 alan: ...)" der ama parantez
   içi liste 15 alan sayar; sözleşme §1 ve Manifest.cs de 15 alandır. Açık alan LİSTESİ esas alındı
   (15 alan) — şemaya alan eklenmedi/çıkarılmadı; "16" task'ta sayım typo'su.
2. **Java rezerve kelimeleri**: JSON `"throws"` → bileşen `throwsList` (`OperationJson`),
   JSON `"extends"` → bileşen `extendsActor` (`ContractActor`); ikisi de `@JsonProperty` ile
   JSON adını birebir korur. Şema değişikliği değildir.
3. **`ExtJson.args`**: `Map<String,JsonNode>` (C# `Dictionary<string,JsonElement>` karşılığı),
   tipli sınıf yazılmadı; eksikse `Map.of()`'a normalize.
4. Loader/GM/TypeEnv yazılmadı (out-of-scope, T1.3/T2.1). Golden dosya yok. CoreTemplate1'e yazma yok.

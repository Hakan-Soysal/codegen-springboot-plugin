# T9.3 — plugin.json + marketplace.json + pack script

## Yapılanlar

1. `plugins/codegen-spring/.claude-plugin/plugin.json` — name `codegen-spring`, version `0.1.0`,
   description `[dsl-generator]` işaretli (CoreTemplate1 `codegen` plugin.json'undan uyarlanmış,
   dotnet/C# referansları Java/Spring Boot'a çevrilmiş: Command/Query/Boundary/Trigger/Subscription
   → Handler/Boundary/Trigger/Consumer/Arrange).
2. Kök `.claude-plugin/marketplace.json` — name `codegen-spring-tools`, tek plugin girdisi
   `codegen-spring` (`source: ./plugins/codegen-spring`, version `0.1.0`, tags Java/Spring Boot'a
   uyarlanmış).
3. `scripts/pack-plugin-bundles.sh` — `mvn -q package` çalıştırır (gen-cli + conformance shaded
   jar'ları üretir), `gen-cli/target/gen-cli.jar` → `plugins/codegen-spring/skills/base-springboot-rest/techgen/gen-cli.jar`,
   `conformance/target/conformance.jar` → `plugins/codegen-spring/skills/base-springboot-rest/conformance/conformance.jar`
   kopyalar (CoreTemplate1 `pack-plugin-bundles.sh`'ın Maven/jar uyarlaması; `dotnet publish` yerine
   `mvn package`, `.dll` glob yerine tek `.jar` dosyası).

## Finding #1 senkronu (T9.1)

`capability.json` (`plugins/codegen-spring/skills/base-springboot-rest/capability.json`):
```
"source": {
  "marketplace": "Hakan-Soysal/codegen-springboot-plugin",
  "install": "/plugin marketplace add Hakan-Soysal/codegen-springboot-plugin && /plugin install codegen-spring@codegen-spring-tools"
}
```
→ `marketplace.json`'un `name` alanı `codegen-spring-tools` (install komutundaki `@codegen-spring-tools`
ile eşleşiyor), plugin `name` alanı `codegen-spring` (capability.json install komutundaki
`codegen-spring@...` ile eşleşiyor). GitHub repo adı (`Hakan-Soysal/codegen-springboot-plugin`)
marketplace.json içinde ayrıca yer almıyor (CoreTemplate1 örneğinde de repo adı marketplace.json'da
tutulmuyor, GitHub üzerindeki fiziksel repo konumunu temsil ediyor) — tutarsızlık yok.

## Doğrulama (gerçek koşum)

- `bash scripts/pack-plugin-bundles.sh` → exit 0. `mvn -q package` gen-cli + conformance modüllerini
  (surefire testleri dahil, H2/Hibernate/Spring context log'ları görüldü) derledi, jar'ları
  `techgen/` ve `conformance/` altına kopyaladı.
- `ls` → `plugins/codegen-spring/skills/base-springboot-rest/techgen/gen-cli.jar` (2.5 MB),
  `plugins/codegen-spring/skills/base-springboot-rest/conformance/conformance.jar` (7.5 MB) — ikisi de var.
- `java -jar techgen/gen-cli.jar` → `kullanım: java -jar gen-cli.jar <manifest.json> <outDir>` (exit 2, argümansız usage — beklenen davranış).
- `java -jar conformance/conformance.jar` → `usage: java -jar conformance.jar <appClasspath> <specsPath>` (exit 2, argümansız usage — beklenen davranış).
- `python3 -m json.tool` üç JSON'da da (`plugin.json`, `marketplace.json`, `capability.json`) VALID.

## Kapsam / git

- `git check-ignore` jar path'lerinde exit 1 (gitignore'da DEĞİL) → CoreTemplate1 emsaliyle (DLL'ler
  `plugins/codegen/skills/base-dotnet-rest/{techgen,conformance}/` altında git-tracked) tutarlı olarak
  jar'lar da commit'e dahil edildi.
- `mvn package` yan etkisi olarak `gen-cli/dependency-reduced-pom.xml` ve
  `conformance/dependency-reduced-pom.xml` (maven-shade-plugin ara-çıktısı) oluştu — kapsam dışı,
  stage EDİLMEDİ (untracked bırakıldı).
- `.claude/settings.local.json` ve `tasks/.pm-state/*` stage EDİLMEDİ (talimat).
- Push YAPILMADI.

# T4.3 Raporu — Trigger (`@trigger.*` ext → SmartLifecycle stub + seam)

**Durum:** DONE · **Tarih:** 2026-07-03 · **Attempt:** 1/3

## Yapılanlar

### `SpringEmitter.java` — op döngüsüne Step 5.6 eklendi (M4 §4.3)

Referans: `docs/referans/gen-dotnet-emisyon-sozlesmesi.md` §1 (`{Op}.Trigger.g.cs`, `:1105-1123`)
+ §2 (seam mekaniği, boş-marker metinleri) — CoreTemplate1 `DotnetEmitter.cs:1105-1140`
`TriggerPartial`/`TriggerLogic` (READ-ONLY okundu) Java/Generation-Gap'e uyarlandı.

- Op'ta `@trigger.{name}` ext'i varsa (`op.op().ext()` filtre `ns=="trigger"`), her trigger için:
  - **Gen-owned** `{op}/{Op}{T}TriggerBase.java` (writeAlways) — abstract class, `SmartLifecycle`
    implement eder; `isRunning()`/`stop()` gen-owned basit-state (`protected volatile boolean
    running`); `public abstract void start();` bildirimi; ctor'da `protected final {Op}Handler
    handler` alanı.
  - **Human seam** `src/.../{Op}{T}Trigger.java` (writeIfAbsent) — Base'i extend eder; `start()`
    gövdesi birebir marker: `throw new UnsupportedOperationException("{opId}{T}Trigger.start:
    doldurulacak");` (.NET `"{op.Id}{T}Trigger.StartAsync: doldurulacak"` paritesi — Java hedefinde
    `SmartLifecycle.start()` senkron olduğu için `StartAsync`→`start`).
  - **Wiring** (`{Module}Wiring.java`) — EXPLICIT `@Bean` kaydı (component-scan YOK, SPEC §12/4
    tam-açık kayıt): `{Op}{T}Trigger` bean'i `{Op}Handler` parametresi alır; `SmartLifecycle` bean'i
    Spring context'e kaydolduğunda `DefaultLifecycleProcessor` otomatik yönetir.
  - **Policy** `trigger-wiring = "SmartLifecycle stub (generator-policy)"` (.NET
    `TriggerPartial:1109` paritesi).
- `T = Naming.pascal(trigger.name())`; trigger sınıf adı `{opId}{T}Trigger` (ör.
  `WriteAuditLogCronTrigger`).

### Rule 9 uyumu — `realized("@trigger.{name}")` TEKRARLANMADI

T3.7'nin `extFields` metodu (satır ~1030-1046) op'un **TÜM** ext ns'lerini (trigger dahil) zaten
döngüyle geziyor ve her biri için `report.realized("@" + e.ns() + "." + e.name(), op.id())` +
`report.policy(e.ns() + "-realization", ...)` çağırıyor — bu, .NET `ExtPartial` (`:1142-1177`)
paritesidir ve `@trigger.cron`'u da kapsar. Bu task'ta eklenen trigger-emisyon bloğu **hiçbir
`report.realized("@trigger...")` çağrısı içermez** — yalnız `report.policy("trigger-wiring", ...)`
(ayrı bir policy anahtarı, `{ns}-realization`'dan farklı) eklenir. `TriggerEmitTest.
trigger_realizedEntry_isExactlyOnce_fromT37ExtLoop` bunu doğruluyor: `@trigger.cron`/`WriteAuditLog`
entry'si tam 1 kez var.

### Değişmeyen dosyalar
- Task metninin ~245. satırındaki `→ realized("@trigger.{name}", opId)` ifadesi COORDINATION
  NOTES rule 9 ile açıkça EZİLDİ (kaynak: T3.7 verifier raporu
  `tasks/.pm-state/verifier-reports/T3-7-attempt-1.json`) — bu executor'da UYGULANMADI, rapor
  ediliyor (sapma DEĞİL, önceden belirlenmiş kural).

## Testler — `TriggerEmitTest.java` (yeni, 8 test)

Fixture canlı örnek: `WriteAuditLog` (`Ops` modülü) `@trigger.cron` (`schedule: "0 0 * * *"`).

1. `writeAuditLog_triggerBase_isEmitted_withSmartLifecycleStubShape` — Base şekli (SmartLifecycle,
   handler alanı, `start()` soyut, `isRunning`/`stop` gen-owned, GEN_HEADER prepend).
2. `writeAuditLog_triggerSeam_isEmitted_withUnsupportedMarker` — seam şekli + birebir marker
   metni + GEN_HEADER YOK (insan-sahipli).
3. `writeAuditLog_triggerSeam_secondEmit_doesNotOverwrite` — seam dosyası elle özelleştirilip
   ikinci `emit()` çağrısından sonra **byte-aynı** kaldığı doğrulandı (WriteIfAbsent kontratı).
4. `writeAuditLog_triggerBase_secondEmit_isRewrittenIfChanged` — TriggerBase deterministik
   (aynı GM → aynı içerik; write-only-if-changed).
5. `opsWiring_hasExplicitTriggerBean` — `OpsWiring.java`'da EXPLICIT `@Bean` + import; `@ComponentScan`
   YOK.
6. `triggerWiringPolicy_isRecorded` — `trigger-wiring` policy anahtarı + değeri.
7. `trigger_realizedEntry_isExactlyOnce_fromT37ExtLoop` — çift-entry OLMADIĞI (rule 9).
8. `generatedApp_realMvnCompile_exitsZero_afterT43` (`@Tag("e2e")`) — üretilen app **gerçek**
   `mvn -q -f <outDir>/pom.xml compile` exit 0.

## Acceptance sonuçları (gerçek koşum)

- Precondition: JDK21 `ls $JAVA_HOME/bin/java` OK; `mvn -q -pl gen-spring -am compile` **exit 0**
  (değişiklik öncesi doğrulandı).
- Değişiklik sonrası `mvn -q -pl gen-spring -am compile` → **exit 0** (çıktı boş, hata yok).
- `mvn -q -pl gen-spring -am test` → **exit 0** (tüm reactor, gen-core + gen-spring).
- Surefire raporları (`gen-spring/target/surefire-reports/*.txt`, gerçek koşum okundu):
  - `AppSkeletonTest`: 4
  - `EmitWriterTest`: 11
  - `GeneratedAppCompileTest`: 7
  - `GuardsEmitTest`: 5
  - `HandlerBaseAuthThrowsConsistencyExtEmitTest`: 9
  - `IdempotencyPaginationEmitTest`: 12
  - `JpaEmitTest`: 6
  - `PlaceholderTest`: 1
  - `PredicateRenderTest`: 9
  - `ProvenanceIoTest`: 9
  - `ResultTypesEmitTest`: 11
  - `SubscriptionEmitTest`: 10
  - **`TriggerEmitTest`: 8** (yeni; Tests run: 8, Failures: 0, Errors: 0, Skipped: 0)
  - **Toplam gen-spring: 102 test, 0 failure, 0 error.**
- DoD madde 1: WriteAuditLog `@trigger.cron` fixture'da TriggerBase + seam üretiyor; ikinci emit
  seam'i EZMİYOR — testlerle (#1-4) doğrulandı.
- DoD madde 2: Compile yeşil (gerçek `mvn` koşumu, hem gen-spring modülü hem üretilen app);
  `trigger-wiring` policy var (#6); `@trigger.cron` realize TAM 1 kez (#7, rule 9).

## Notlar / sapmalar

- **Kasıtlı sapma (rule 9 gereği, task metninden DEĞİL):** Task'ın kısa-format metni "→
  `realized("@trigger.{name}", opId)`" diyor; bu UYGULANMADI çünkü T3.7 (commit `9d836ca`)
  `ExtPartial` paritesiyle TÜM ext ns'lerini (trigger dahil) zaten realize ediyor — ikinci bir
  `realized()` çağrısı çift-entry (INV-7 census'ta yanlış pozitif çoğullama) yaratırdı. Kaynak:
  COORDINATION NOTES rule 9 + `tasks/.pm-state/verifier-reports/T3-7-attempt-1.json`.
- SmartLifecycle `isRunning`/`stop` "basit state" yorumu: `protected volatile boolean running`
  alanı + `isRunning()` getter + `stop()`'ta `running=false`. Alan `protected` (accessor metodu
  YOK) — human seam ileride gerçek implementasyonda doğrudan `running = true` atayabilir (bkz.
  test #3'teki özelleştirilmiş seam örneği). Bu, .NET'in `IHostedService.StopAsync()=>
  Task.CompletedTask` gen-owned no-op'una en yakın Java/Spring karşılığı (IHostedService'te
  `IsRunning` kavramı yok; `SmartLifecycle` gerektirdiği için minimal state eklendi).
  Task metninde bu detay netleştirilmediği için üstteki minimal-state yorumu tercih edildi —
  başka bir tasarım isteniyorsa (ör. `AtomicBoolean` + `markStarted()` accessor) PM'e bildirilmeli.
- `HandlerBase` ctor'una PARAM EKLENMEDİ (rule 8) — Trigger kendi ayrı SmartLifecycle bean'i,
  kendi ctor'unda `{Op}Handler` alanı.
- Component-scan açılmadı; Wiring bean'leri TAM-AÇIK (SPEC §12/4).
- CoreTemplate1: yalnız `src/Gen.Dotnet/DotnetEmitter.cs` (TriggerPartial/TriggerLogic bölümü)
  okundu, hiçbir yazma yapılmadı.
- Kapsam dışı dosyalara dokunulmadı (`git diff --stat`: yalnız `SpringEmitter.java` +
  `TriggerEmitTest.java`); `tasks/.pm-state/`, `.claude/settings.local.json` bu commit'e dahil
  edilmedi.

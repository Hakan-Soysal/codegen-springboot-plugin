# T4.4 Raporu — Boundary externals + client seam + boundary validation + Uncharted

**Durum:** DONE · **Tarih:** 2026-07-03 · **Attempt:** 1/3

## Yapılanlar

### `SpringEmitter.java` — dört yeni emisyon + ctor-threading + Bootstrap kaydı

Referans: `docs/referans/gen-dotnet-emisyon-sozlesmesi.md` §1 Boundary (`:913-955`/`:959-974`/
`:977-1014`) + Uncharted (`:1017-1071`) + §7 disposition tablosu — CoreTemplate1
`DotnetEmitter.cs:913-1071` (`BoundaryFile`/`BoundaryClientLogic`/`BoundaryValidation`/
`UnchartedFile`, READ-ONLY okundu) Java/Generation-Gap'e uyarlandı.

**Emit sırası** (`emit()` içinde, PersistenceConfig'ten sonra, modül döngüsünden önce — SPEC §6.3
"gen/ kökü (global)" tablosuyla uyumlu, root-level/modül-bağımsız):

1. **Step 5.1 — `gen/java/app/boundary/{Ext}.java`** (writeAlways) — `public interface {Ext}`, her
   boundary-op `{RetJavaTip} {opCamel}({params});`; serving varsa metot üstü yorum (`// serving:
   {raw} — transport client sorumluluğu`, birebir task formatı). `realized("external", name)`, her
   op `realized("boundary-op", "{ext}.{op}")`, serving başına
   `realized("serving", "{ext}.{op}:{proto}")`.
2. **Step 5.2 — `src/main/java/app/boundary/{Ext}Client.java`** (writeIfAbsent, human seam) —
   `{Ext}` arayüzünü implemente eder; her metot birebir marker: `"{Ext}.{op}: doldurulacak"`.
   `GeneratedBootstrap`'ta EXPLICIT `@Bean {Ext} {ext}Client()` (her external için, kullanılıp
   kullanılmadığına bakılmaksızın — TAM-AÇIK KAYIT, SPEC §12/4).
3. **Step 5.3 — `{pkg}/{owner}{OpPascal}Validation.java`** (writeAlways; hem `boundary/` hem
   `uncharted/` paketi için ortak `boundaryValidationJava` helper'ı) — boundary-op'ta validation
   varsa `validation{i}({tipli input record})`, T3.5 `JavaPredicateRenderer` ile; tip çözümü
   **boundary-op signature paramlarından** (`resolveType` = `b.signature().params()` içinde ad
   eşleşmesi → `p.type()`; bulunamazsa renderer'ın kendi hints→Decimal son-çare zinciri devreye
   girer). `realized("validation", "{owner}.{op}")` (≥1 guard başarılıysa; Guards T3.5 ile aynı
   `#ValidationN` compound-id deseni, `BuildReport.idMatches` ile census kapsanır).
4. **Step 5.4 — `gen/java/app/uncharted/{Name}.java`** (writeAlways) — stub `interface {Name}` +
   paket-özel (`final class`, **public DEĞİL**) `{Name}StubClient` — `UnsupportedOperationException`
   mesajı **"doldurulacak" substring'i İÇERMEZ** (gen-owned WriteAlways; global rule #6/INV-S).
   OWNED entity → ayrı dosya `gen/java/app/uncharted/{Name}{Entity}.java` (düz POJO, `@Entity` YOK,
   `jakarta.persistence` import YOK; optimistic concurrency → plain `version` alanı, `@Version`
   YOK). OWNED type → ayrı dosya `gen/java/app/uncharted/{Name}{TypeId}.java` (enum/record, aynı
   `{Name}{TypeId}` adlandırma). `realized("uncharted", name)` +
   `policy("uncharted-realization", "call-adapter stub + owned model (generator-policy)")` +
   boundary-op/serving alt-kayıtları (census şablonu `Completeness.addBoundaryOp` ile birebir);
   validation aynı `boundaryValidationJava` helper'ıyla `uncharted/` paketinde ayrı dosyada.
5. **Ctor-threading (Step 5.2, M4 standing rule #8)** — yeni `boundaryDepsForOp(op, gm)` helper'ı:
   `gm.callEdges()` içinde `from==op.id() && kind=="external"` olan edge'lerin `to.system()`'i
   `gm.externals()`'a eşlenir (ordinal, `TreeSet<String>` ile isim-sıralı). Sonuç, **üç senkron
   noktada** EN SONA eklendi (repos → idempotent → events → **boundary**):
   - `handlerBaseJava`: alan + ctor param + assign (emits/EventBus bloğundan hemen sonra).
   - `humanHandlerJava`: ctor param + `super(...)` arg (aynı konumda).
   - Wiring Step 5.5 döngüsü: bean param + bean dönüş-arg + `import app.boundary.{Ext};`.
   **Yalnız `kind=="external"` callEdge'ler ctor'a bağlanır** — uncharted callEdge'ler (task §7 out
   of scope: "callEdges census kaydı — T4.5") ctor-threading DIŞI bırakıldı; fixture'da
   `WriteAuditLog→LegacyLedger` (kind=uncharted) bu yüzden `WriteAuditLogHandlerBase`'e HİÇBİR alan
   eklemiyor (test `writeAuditLogHandlerBase_hasNoBoundaryField_unchartedIsNotWiredIntoCtor` ile
   doğrulandı).

### Yeni private helper'lar (SpringEmitter.java sonu, "Boundary"/"Uncharted" bölümleri)

`boundaryDepsForOp`, `boundaryOpImports`, `boundaryOpParams`, `boundaryFile`, `boundaryClientJava`,
`boundaryValidationJava` (ortak, external+uncharted), `unchartedFile`, `unchartedEntityJava`
(mevcut `entityFieldImports`/`appendAccessors` yeniden kullanıldı), `unchartedTypeJava` (mevcut
`typeFieldImports` yeniden kullanıldı).

### Mevcut testlerde ctor-sıra güncellemesi (kaçınılmaz — CreateInvoice fixture'ı hem T4.1
idempotent hem T4.2 emits hem şimdi T4.4 boundary'e sahip)

`CreateInvoice→PaymentGateway` callEdge (kind=external) fixture'da zaten mevcuttu (T2.x'ten beri);
bu yüzden `CreateInvoiceHandlerBase`/`Handler`/`BillingWiring` ctor'u artık `..., EventBus eventBus,
PaymentGateway paymentGateway)` ile bitiyor. Önceki milestone'ların ctor-sonu birebir substring
assert'leri (T4.1/T4.2 testleri) bu yeni parametreyi hesaba katmıyordu — standing rule #8 açıkça bu
güncellemeyi (kendi testimde VEYA mevcut assert'te) istedi. Güncellenen dosyalar:
- `GeneratedAppCompileTest.java` (2 assert: HandlerBase ctor imzası, BillingWiring bean+return).
- `IdempotencyPaginationEmitTest.java` (4 assert: HandlerBase ctor, human Handler ctor, Wiring bean
  param, Wiring return-arg).
- `SubscriptionEmitTest.java` (2 yer: `EventBus eventBus)` substring'i → `EventBus eventBus,
  PaymentGateway paymentGateway)`; ctor-SIRA assert'inin `expected` listesi
  `[invoiceRepository, idempotencyStore, eventBus]` → `[..., paymentGateway]` genişletildi).

Hiçbiri DAVRANIŞ değişikliği DEĞİL — yalnızca fixture'ın zaten sahip olduğu callEdge'in artık
gerçekten emit edilmesinin sonucu (önceden T4.4 yokken bu callEdge hiçbir etkiye sahip değildi).

## Testler — `BoundaryEmitTest.java` (yeni, 13 test)

Fixture canlı örnekler: `PaymentGateway` external (`charge`/`refund`, charge validation'lı,
`CreateInvoice→PaymentGateway.charge` kind=external) + `LegacyLedger` uncharted (`post`,
`WriteAuditLog→LegacyLedger.post` kind=uncharted, entity `LedgerEntry`, type `LedgerKind` enum).

1. `paymentGatewayInterface_isEmitted_withMethodsAndServingComment` — arayüz şekli, GEN_HEADER,
   `String charge(BigDecimal amount)`/`boolean refund(String chargeId)`, serving yorumu.
2. `paymentGatewayClientSeam_isEmitted_withVerbatimMarker` — human seam, GEN_HEADER YOK, marker
   birebir (`"PaymentGateway.charge: doldurulacak"` / `"PaymentGateway.refund: doldurulacak"`).
3. `paymentGatewayClientSeam_secondEmit_doesNotOverwrite` — ikinci emit'te byte-aynı kalır.
4. `generatedBootstrap_hasExplicitPaymentGatewayClientBean_noComponentScan` — EXPLICIT `@Bean`,
   `@ComponentScan` YOK.
5. `paymentGatewayChargeValidation_isEmitted_withCompareToForm` — `validation0` birebir:
   `(input.amount().compareTo(new BigDecimal("0")) > 0)`.
6. `createInvoiceHandlerBase_hasPaymentGatewayField_fromCallEdge` — HandlerBase/human
   Handler/Wiring üç-nokta ctor-senkron (EN SONA).
7. `writeAuditLogHandlerBase_hasNoBoundaryField_unchartedIsNotWiredIntoCtor` — **negatif**:
   uncharted callEdge ctor'a BAĞLANMAZ.
8. `legacyLedgerFile_isEmitted_withStubInterfaceAndClient` — stub arayüz+client, GEN_HEADER,
   **"doldurulacak" substring'i YOK** (gen-owned ayrımı).
9. `unchartedStubClient_isNotHumanSeam_noFileUnderSrc` — **negatif** (DoD şartı): `src/main/java/
   app/uncharted/` altında HİÇBİR dosya/dizin yok.
10. `legacyLedgerEntity_isEmitted_asPlainPojo` — `LegacyLedgerLedgerEntry` POJO şekli.
11. `unchartedEntity_isPlainPojo_notJpaEntity` — **negatif** (DoD şartı): `@Entity` YOK,
    `jakarta.persistence` import YOK.
12. `buildReport_realizesBoundaryAndUnchartedConstructs` — 5 realized entry + `uncharted-realization`
    policy değeri birebir.
13. `generatedApp_realMvnCompile_exitsZero_afterT44` (`@Tag("e2e")`) — üretilen app **gerçek**
    `mvn -q -f <outDir>/pom.xml compile` exit 0.

DoD şartı "≥9 test, iki negatif assert dahil" karşılandı: 13 test, ≥2 negatif assert
(#7, #9, #11 — üç negatif).

## Acceptance sonuçları (gerçek koşum)

- Precondition: JDK21 `ls $JAVA_HOME/bin/java` OK; `mvn -q -pl gen-spring -am test` (T4.3 sonrası
  temel) **exit 0** (değişiklik öncesi doğrulandı, 102 test).
- Değişiklik sonrası `mvn -q -pl gen-spring -am compile` → **exit 0**.
- İlk `mvn -q -pl gen-spring -am test` koşumunda **7 pre-existing test kırıldı** (beklenen —
  standing rule #8'in öngördüğü durum); yukarıdaki 3 test dosyası güncellendi.
- Güncelleme sonrası `mvn -q -pl gen-spring -am test` → **exit 0**, sıfır failure.
- Surefire raporları (gerçek koşum okundu):
  - **`BoundaryEmitTest`: 13** (yeni; Tests run: 13, Failures: 0, Errors: 0, Skipped: 0)
  - Diğer tüm mevcut test sınıfları (T0-T4.3): değişmedi / güncellenen 3 dosya dahil hepsi yeşil.
  - **Toplam gen-spring: 115 test, 0 failure, 0 error** (`grep -rh "Tests run:"
    gen-spring/target/surefire-reports/*.txt` toplamı).
- `generatedApp_realMvnCompile_exitsZero_afterT44` dahil TÜM `@Tag("e2e")` testleri
  (`mvn test` tag-filtreleme yapmıyor, hepsi koşuyor) **exit 0**.
- Manuel doğrulama (gerçek emit çıktısı okundu, `git diff` DIŞI geçici dizine): `PaymentGateway.java`,
  `PaymentGatewayClient.java`, `PaymentGatewayChargeValidation.java`, `LegacyLedger.java`,
  `LegacyLedgerLedgerEntry.java`, `LegacyLedgerLedgerKind.java`, `LegacyLedgerPostValidation.java`,
  `GeneratedBootstrap.java` içerikleri tek tek `cat` ile gözle doğrulandı — beklenen şekille birebir.

## Notlar / sapmalar (verifier için)

1. **SPEC/task iç-tutarsızlığı (ÇÖZÜLMEDİ, raporlanıyor — global rule #4):** .NET referans deseni
   (`DotnetEmitter.cs:934`, `ExtComment(p.Ext, ...)`) boundary-op **param-seviyesi** `ext` (ör.
   fixture'daki `PaymentGateway.charge.amount` → `@sensitivity.tag`, `LegacyLedger.post.amount` →
   `@ucparam.tag`) için yorum + `report.realized("@ns.name", "{ext}.{op}.{param}")` üretir; ayrıca
   `Completeness.addBoundaryOp` (gen-core, mevcut kod) bu ext'leri **census'a dahil ediyor**
   (`addExt(x, p.ext(), extName + "." + b.id() + "." + p.name())`). **T4.4 task dosyasının Step
   5.1/5.4 action metni bu param-ext işlemeyi HİÇ anmıyor** (yalnız serving+realized("external"/
   "boundary-op"/"serving") sayıyor). Task metnine BİREBİR uyup bu census pair'lerini
   REALİZE ETMEDİM (kapsam-dışı ekleme yapmadım — global rule "yalnız istenen şeyi yap"). **Sonuç:**
   gen-cli'de Completeness gate ileride devreye alınırsa, `("@sensitivity.tag",
   "PaymentGateway.charge.amount")` ve `("@ucparam.tag", "LegacyLedger.post.amount")` census
   pair'leri **silentDrop** olarak işaretlenecek (gen-cli henüz bu gate'i çağırmıyor — şu an davranışsal
   etkisi YOK, ama gelecekte INV-7 ihlali üretecek). PM/sonraki task bu boşluğu (muhtemelen T4.5
   veya ayrı bir düzeltme) kapatmalı.
2. **Uncharted TYPE'lar için `realized()` çağrısı YOK** — `Completeness.census()` (gen-core, mevcut
   kod) uncharted döngüsünde yalnız `uncharted`/`boundary-op`/`validation`/`serving`/`concurrency`
   izliyor; `u.types()`/`u.entities()`'in kendisi (ext hariç) census'ta AYRI bir construct değil.
   Bu yüzden `unchartedTypeJava`/`unchartedEntityJava` (concurrency hariç) hiçbir `report.realized()`
   çağırmıyor — referans §7 tablosuyla birebir (fazladan/hayalet realized entry EKLENMEDİ).
3. **`{Name}StubClient` `public` DEĞİL** (paket-özel `final class`) — dosyanın tek public tipi
   `{Name}` arayüzü (Java "dosya adı = public tip adı" kısıtı; `EventBus.java`/`IdempotencyStore.java`
   ile aynı gen-owned çoklu-sınıf-tek-dosya deseni). Task hiçbir yerde `{Name}StubClient`'ın dışarıdan
   erişilmesini istemiyor (ctor-threading yalnız external için var) — bu yüzden erişim kısıtlaması
   davranışı etkilemiyor.
4. **Bean konumu:** `{Ext}Client` bean'i modül-Wiring'de DEĞİL, **`GeneratedBootstrap`'ta** (root,
   `EventBus`/`IdempotencyStore` ile aynı konum) — external cross-module bir kaynak olduğu için
   (birden fazla modülün op'u aynı external'ı çağırabilir); tek bir yerde kayıtlı olması TAM-AÇIK
   KAYIT ilkesiyle daha tutarlı. Task metni "Wiring/Bootstrap'ta" diyerek ikisini de olası bıraktığı
   için bu bir spec-sapması DEĞİL, iki olası yorumdan biri.
5. Component-scan açılmadı; tüm yeni bean'ler EXPLICIT (SPEC §12/4).
6. CoreTemplate1: yalnız `src/Gen.Dotnet/DotnetEmitter.cs:900-1071` (Boundary/Uncharted bölümü)
   okundu, hiçbir yazma yapılmadı.
7. Kapsam dışı dosyalara dokunulmadı — `git diff --stat` yalnız: `SpringEmitter.java` (main),
   `BoundaryEmitTest.java` (yeni), `GeneratedAppCompileTest.java`/`IdempotencyPaginationEmitTest.java`/
   `SubscriptionEmitTest.java` (ctor-sıra substring güncellemesi, madde yukarıda gerekçelendirildi).
   `tasks/.pm-state/`, `.claude/settings.local.json` bu commit'e dahil edilmedi.

# T10.1 — E2E senaryo + README + SPEC sync (M10, proje SON task'ı)

## Yapılanlar

1. **`scripts/e2e-demo.sh`** — uçtan-uca el senaryosunu tek script'e döktü:
   - `mvn -q package` (gen-cli.jar + conformance.jar taze),
   - temiz bir `mktemp -d` çalışma dizinine `java -jar gen-cli.jar fixtures/manifest.json <outDir>`
     ile generate,
   - üretilen `GetInvoice` seam'ini (`src/main/java/app/billing/getinvoice/GetInvoiceHandler.java`)
     örnek bir iş mantığıyla doldurdu: `invoiceRepository.findById(id)` → bulunursa `Success`,
     bulunamazsa `NotProcessable("NotFound", ...)` (GetInvoice.throws==[] fixtures/manifest.json'da
     — bu kod SPEC'in dayattığı bir sözleşme DEĞİL, demo amaçlı örnek),
   - üretilen app'i `mvn -q -f <outDir>/pom.xml compile` ile gerçekten derledi,
   - basit bir conformance spec'i (`{construct:"smoke", opId:"GetInvoice", act:{call:"GetInvoice",
     with:{id:"inv-does-not-exist", includeVoid:false}}, assert:{resultType:"NotProcessable",
     code:"NotFound"}}`) `java -jar conformance.jar <appClasspath> <specsPath>` ile koştu.
   - Script standalone çalışır: JAVA_HOME/PATH export'u script içinde gömülü. Geçici çalışma dizini
     `trap` ile temizlenir. Golden dosyalara dokunmaz.
2. **`README.md`** (kök, yeni) — CoreTemplate1 README yapısından uyarlandı: Kurulum (yerel
   geliştirme: mvn test/package, pack-plugin-bundles.sh, e2e-demo.sh) + Kurulum (Claude Code
   plugin: marketplace add/install/update — `plugins/.../capability.json` ve
   `.claude-plugin/marketplace.json`'daki gerçek isimlerle: marketplace `codegen-spring-tools`,
   plugin `codegen-spring`) + CLI kullanımı (gen-cli + conformance, exit sözleşmeleri) + E2E demo
   açıklaması + İçerik tablosu + Pair mimarisi (üreteç/doldurucu/Generation Gap/TAM-AÇIK bean) +
   Doğrulama/test stratejisi.
3. **`SPEC.md` sync** — §6.5'e (Java predicate renderer) eksik bir kural eklendi: temporal
   (`Date`→`LocalDate`, `DateTime`→`Instant`) alanların da `compareTo` formuna alındığı
   (T6.3-FIX #3, commit `00786dd`) SPEC'te YOKTU; §6.5 yalnız BigDecimal/String/primitif
   kurallarını listeliyordu. Gerçek koda göre eklendi (aşağıda "SPEC drift" bölümü).

## SPEC drift bulgusu ve düzeltme

Görev talimatındaki 4 aday davranış tek tek kod ile karşılaştırıldı:

| Aday | Bulgu | Sonuç |
|---|---|---|
| `finishAndPrune` imzası | `EmitWriter.finishAndPrune()` parametresiz; SPEC hiçbir yerde bir imza iddia etmiyor (yalnız genel INV-D/prune davranışı anlatılıyor, §9 "prune-keeps-human") | **Drift YOK** — SPEC zaten imza-nötr |
| Temporal `compareTo` | `JavaPredicateRenderer` (T6.3-FIX #3, commit `00786dd`) `LocalDate`/`Instant` karşılaştırmalarını da `compareTo` formuna alıyor; SPEC §6.5 yalnız BigDecimal/String/primitif'i listeliyordu | **Drift VAR** → §6.5'e madde eklendi |
| `'test'` census realize | `SpringEmitter.emitOneTest` her emit edilen (all-Single) test için `report.realized("test", "{Scope}_{testId}")` çağırıyor (commit `47d554c`, T7.1-PARITE); `docs/referans/gen-dotnet-emisyon-sozlesmesi.md:67` bunu zaten belgeliyor, SPEC §1 kapsamda "üretilen-test iskeleti (TestPlan)" olarak sayıyor, `Completeness.census()` (INV-7 ölçüm noktası) yalnız manifest'ten türer — `"test"` bu census'a hiç GİRMİYOR (ek/bilgi amaçlı entry) | **Drift YOK** — referans+§1 kapsamı zaten kapsıyor, INV-7 tanımıyla çelişki yok |
| Boundary/op param-ext | `typeLevelExtComment` genel yardımcısı; census construct adı her zaman `@{ns}.{name}` (SPEC §5'teki `@{ns}.{name}` şablonuyla birebir); owner farklı bağlamlarda değişir (`{op}.{param}`, `{ext}.{op}.{param}`, `{entity}.{field}`, bare `{module}`/`{entity}`/`{type}` adı) — SPEC §5'in owner-id listesi (`{ext}.{op}`, `{op}:{proto}`, `{op}->{err}`, `{en}.{f}`, `@{ns}.{name}`) referans dokümandaki gibi **örnekseyici**, iddialı-kapsayıcı değil (referans `:264`'te de aynı üç örnekle sınırlı) | **Drift YOK** — construct-adı şablonu (`@{ns}.{name}`) birebir uyuyor; owner çeşitliliği zaten "ext her yere eklenebilir" doğasından geliyor, referans da exhaustive değil |

Sonuç: **1 gerçek drift** (temporal compareTo), SPEC §6.5'e eklendi; diğer 3 aday incelendi ve
mevcut SPEC/referans metniyle çelişmediği doğrulandı (sessiz sapma YOK, hepsi gerekçeli).

## Doğrulama (gerçek koşum)

- `bash scripts/e2e-demo.sh` → **exit 0**. Çıktı (ilgili satırlar):
  ```
  == [1/5] mvn -q package (gen-cli.jar + conformance.jar taze) ==
  == [2/5] temiz dizine generate (java -jar gen-cli.jar) ==
  emit → /var/folders/.../tmp.mpHOadXYWL/out  (clean=false, constructs=78, silentDrops=0)
  == [3/5] GetInvoice seam'ini örnek impl ile doldur ==
  == [4/5] mvn -q compile (üretilen app) ==
  == [5/5] basit conformance spec'i koş (java -jar conformance.jar) ==
  [PASS] smoke/GetInvoice: resultType='NotProcessable', code='NotFound'
  conformance: 1 pass, 0 fail, 0 skip
  == e2e-demo: TAMAM (conformance PASS görüldü) ==
  ```
  (Konsol Spring/Hibernate/HikariCP INFO log satırları elenmiştir; script tam log'u basar.)
- README'deki komutlar tek tek gerçekten koşuldu:
  - `mvn -q test` (kökten) → exit 0.
  - `mvn -q package` → exit 0 (gen-cli.jar 2.5MB, conformance.jar 7.5MB üretildi).
  - `bash scripts/pack-plugin-bundles.sh` → exit 0; `plugins/codegen-spring/skills/base-springboot-rest/{techgen/gen-cli.jar,conformance/conformance.jar}` tazelendi (byte-aynı — git status'ta değişiklik göstermedi, reproducible build).
  - `bash scripts/e2e-demo.sh` → exit 0 (yukarıda).
- `mvn -q package`/`pack-plugin-bundles.sh` koşumları `conformance/dependency-reduced-pom.xml` ve
  `gen-cli/dependency-reduced-pom.xml` (maven-shade-plugin yan-ürünü) bıraktı — bunlar task
  kapsamı DIŞI, doğrulama sonrası silindi (`git status` temiz, yalnız kapsam dosyaları kaldı).

## Kapsam dışı / dokunulmayan

- `tasks/IMPLEMENTATION-PLAN.md`, `tasks/T-*.md`, `tasks/.pm-state/` — dokunulmadı.
- CoreTemplate1 — yalnız okundu (`README.md`), hiçbir yazma yapılmadı.
- `.claude/settings.local.json` — stage edilmedi (pre-existing untracked, bu task'ın kapsamı değil).

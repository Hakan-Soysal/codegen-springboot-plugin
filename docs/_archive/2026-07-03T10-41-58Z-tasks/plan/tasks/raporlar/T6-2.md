# T6.2 Raporu — Emit davranış + LatentConstruct + Report test portu

**Durum:** DONE · **Tarih:** 2026-07-04 · **Attempt:** 1/3

## Yapılanlar

`docs/referans/conformance-testler-skill-sozlesmesi.md` §B (EmitTests/LatentConstructTests/
ReportTests/CompletenessTests davranış listesi) + CoreTemplate1 `tests/Gen.Tests/{EmitTests,
LatentConstructTests,ReportTests,CompletenessTests}.cs` (READ-ONLY okundu) baz alınarak, gen-spring'in
T3.x/T4.x/T5.x/T6.1'de ZATEN kurduğu geniş fixture-tabanlı test suite'i (BoundaryEmitTest,
GuardsEmitTest, JpaEmitTest, ResultTypesEmitTest, IdempotencyPaginationEmitTest, SubscriptionEmitTest,
TriggerEmitTest, HandlerBaseAuthThrowsConsistencyExtEmitTest, GeneratedAppCompileTest, ZeroDropTest,
CharacterizationTest, MainTest) taranarak §B maddelerinin HANGİLERİNİN zaten dolaysız kapsandığı,
hangilerinin gerçekten test-edilmemiş bir dal/kombinasyon olduğu belirlendi. Yeni testler yalnız
GERÇEKTEN eksik davranışlara yazıldı (kör mükerrer port yapılmadı — anti-pattern §8 "duplicate
scaffold" uyarısı).

### Yeni dosyalar (4)

1. `gen-spring/src/test/java/techgen/spring/EmitTests.java` — **8 test**: `dbProvider` yapılandırma
   yolları (SPEC §6.6; daha önce gen-spring seviyesinde HİÇ doğrudan test edilmemişti — yalnız
   gen-cli `MainTest`'te 2 senaryo vardı): null-config seam, `GenConfig(null,null)` aynı davranış,
   h2/inmemory/postgres/sqlserver whitelist → realized + doğru driver bağımlılığı + datasource
   yorumu, bilinmeyen provider → Unsupported + seam fallback + tam-1-kez raporlama, h2 tam-1-kez
   raporlama simetri testi.
2. `gen-spring/src/test/java/techgen/spring/LatentConstructTests.java` — **15 test**: fixture'ı
   `with`-benzeri kopya kurucularla (gen-core record'ları immutable — canonical-constructor tabanlı
   private static helper'lar: `withOp`/`withEntity`/`withConsistency`/`withServing`/`withVisibility`/
   `withValidation`/`withPagination`/`withSourceOfTruth`/`withField`) genişleterek fixture'ın DOĞAL
   tetiklemediği dalları hedefler: consistency=eventual (CreateInvoice), serving=queue (CreateInvoice,
   grpc zaten ZeroDropTest'te), visibility=internal normalde-exposed bir op üzerinde (GetInvoice),
   guardRef normalde-taşımayan bir op üzerinde (CreateInvoice), pagination=offset (ListInvoices,
   fixture yalnız cursor), sourceOfTruth normalde-taşımayan bir entity üzerinde (Invoice.customerId).
   Ayrıca fixture'ın DOĞAL taşıdığı ama başka hiçbir testte somut emit-çıktısı olarak
   doğrulanmamış 6 annotation-site'ı (module/entity/type-field/op-param/boundary-op-param/
   uncharted-op-param ext + deployable-level ext) ve deployable host-topolojisi + note→Javadoc
   yorumunu MUTASYONSUZ doğrudan fixture'dan doğrular.
3. `gen-spring/src/test/java/techgen/spring/CompletenessTests.java` — **2 test**: `KnownDebt`
   ratchet (boş allowlist) — hem "şu an temiz" (fixture emit → 0 drop-allowlist-dışı) hem de
   ratchet mekanizmasının GERÇEKTEN çalıştığının kanıtı (manifest'e emitter'ın hiç ziyaret etmediği
   yapay bir construct eklenince test KIRILIYOR — yalnız pozitif değil negatif kanıt da var).
4. `gen-core/src/test/java/techgen/core/report/ReportTests.java` — **2 test**: referans-metod-adı
   izlenebilirliği için `Records_status_and_is_clean_only_when_all_realized` 1:1 portu (mevcut
   `ReportTest` ile örtüşür, ucuz+zararsız) + gerçek eksik: "silentDrops diye ayrı bir JSON alanı
   YOK — drop'lar da `constructs[].status` ile taşınır" iddiasının doğrudan doğrulanması.

**Toplam yeni test: 27** (EmitTests 8 + LatentConstructTests 15 + CompletenessTests 2 +
gen-core ReportTests 2 = 27 ≥ 25 DoD eşiği).

## Referans §B kapsama matrisi

### EmitTests (13 metod, C#)

| C# metod | Java karşılığı |
|---|---|
| Emitted_app_compiles_and_gate_has_no_silent_drops | COVERED-EXISTING — `ZeroDropTest.fixtureEmit_hasZeroSilentDrops` + `GeneratedAppCompileTest.generatedApp_realMvnCompile_exitsZero` |
| Emitted_app_with_sqlite_provider_compiles | NEW (whitelist Java'da sqlite değil h2/postgres/sqlserver/inmemory) — `EmitTests.dbProviderH2_*`/`dbProviderPostgres_*`/`dbProviderSqlserver_*` |
| Operation_file_has_request_record_and_partial_handler | COVERED-EXISTING — `GeneratedAppCompileTest.handlerBase_hasRepositoryFieldAbstractExecute_*`, `humanSeam_*` |
| Entity_emits_ef_class_with_rowversion_and_dbcontext | COVERED-EXISTING (JPA eşdeğeri) — `JpaEmitTest.invoiceEntity_*`, `repositories_*`, `persistenceConfig_*` |
| Event_record_bus_and_auth_metadata_emitted | COVERED-EXISTING — `ResultTypesEmitTest.events_*`/`eventBus_*`, `HandlerBaseAuthThrowsConsistencyExtEmitTest.*hasAuthSection` |
| Paginated_query_returns_page_and_takes_cursor_size | COVERED-EXISTING — `IdempotencyPaginationEmitTest.listInvoices_*` |
| Boundary_idempotent_ext_and_policies_emitted | COVERED-EXISTING — `BoundaryEmitTest.*`, `IdempotencyPaginationEmitTest.*`, `ZeroDropTest.fixtureEmit_hasAllRequiredPolicyKeys` |
| Error_catalog_and_throws_binding_emitted | COVERED-EXISTING — `ResultTypesEmitTest.errors_*`, `HandlerBaseAuthThrowsConsistencyExtEmitTest.*hasThrowsSection` |
| ResultHttp_exposes_override_hook | COVERED-EXISTING — `ResultTypesEmitTest.resultHttp_fileEmitted_withToHttpAndOverrideHook` |
| DbProvider_config_emits_config_by_reference_registration | NEW — `EmitTests.dbProviderH2_*`/postgres/sqlserver (datasource yorumu assert'i) |
| DbProvider_config_adds_provider_package_to_props | NEW (gen-spring seviyesinde ilk kez) — `EmitTests.dbProviderH2/Postgres/Sqlserver_*` (gen-cli `MainTest.genConfigH2_addsH2Dependency_exitsZero` zaten vardı, mükerrer değil — farklı modül/API yüzeyi) |
| Null_config_keeps_empty_provider_seam | NEW — `EmitTests.nullConfig_*`, `genConfigWithNullDbProvider_*` |
| Unknown_provider_recorded_unsupported_with_seam_fallback | NEW (gen-spring seviyesinde) — `EmitTests.dbProviderUnknown_isUnsupported_withSeamFallbackComment_inParentPom` (gen-cli `MainTest.genConfigUnknownProvider_*` zaten vardı) |
| Logic_file_is_preserved_on_regeneration | COVERED-EXISTING — `CharacterizationTest.removed_operation_prunes_generated_but_keeps_human_logic`, `TriggerEmitTest.*secondEmit_doesNotOverwrite`, `BoundaryEmitTest.*secondEmit_doesNotOverwrite` |
| Generated_g_files_are_byte_deterministic | COVERED-EXISTING — `CharacterizationTest.generated_tree_is_byte_deterministic` |

### LatentConstructTests (~17 metod, C#)

| C# konu | Java karşılığı |
|---|---|
| Consistency eventual→outbox | NEW — `LatentConstructTests.consistency_eventualRisk_emitsOutboxComment_andPolicy` |
| Deployable→host topolojisi | NEW (fixture direkt, ilk kez içerik-doğrulaması) — `deployable_topologyClass_mapsUnitsPerDeployable` |
| @http/@trigger→target stubs | COVERED-EXISTING (fixture'da WriteAuditLog zaten ikisini birden taşıyor) — `TriggerEmitTest.*`, `HandlerBaseAuthThrowsConsistencyExtEmitTest.writeAuditLog_handlerBase_hasExtSection_withHttpBindingPolicy` |
| Ext her annotation-site'ta (module/type/type-field/entity/op-param) | Type-level COVERED-EXISTING (`ResultTypesEmitTest.types_auditMetaTypeLevelExt_*`); module/entity/type-field/op-param/boundary-op-param/uncharted-op-param/deployable NEW (ilk kez somut-çıktı doğrulaması) — `LatentConstructTests.extModuleLevel_*`, `extEntityLevel_*`, `extTypeFieldLevel_*`, `extOpParamLevel_*`, `extBoundaryOpParamLevel_*`, `extUnchartedOpParamLevel_*`, `deployableExt_*` |
| Subscription→consumer wiring | COVERED-EXISTING (fixture) — `SubscriptionEmitTest.*` |
| @internal→route bastırma | NEW (normalde-exposed bir op üzerinde; fixture'daki tek internal op zaten serving=[] idi) — `LatentConstructTests.visibility_internal_suppressesRoute_onNormallyExposedOp` |
| Boundary-op serving+validation (external) | COVERED-EXISTING (fixture zaten PaymentGateway.charge'da serving+validation taşıyor) — `BoundaryEmitTest.paymentGatewayChargeValidation_isEmitted_withCompareToForm`, `buildReport_realizesBoundaryAndUnchartedConstructs` |
| Serving @grpc/@queue→explicit Unsupported | grpc COVERED-EXISTING (`ZeroDropTest.grpcServing_isUnsupported_notSilentDrop`); queue NEW — `LatentConstructTests.serving_queue_recordedUnsupported_notSilentDrop` |
| GuardRef→predicate yorumu | NEW (farklı bir op üzerinde — fixture'da yalnız WriteAuditLog'ta vardı, `GuardsEmitTest` bunu zaten doğruluyordu) — `LatentConstructTests.guardRef_onDifferentOp_emitsPredicateCommentAndPolicy` |
| GET route-dışı query param binding | COVERED-EXISTING (fixture zaten GetInvoice.includeVoid) — `GeneratedAppCompileTest.bindAsymmetry_postRequestBody_vs_getPathVariableAndRequestParam` |
| Pagination offset stratejisi + size | NEW (fixture yalnız cursor) — `LatentConstructTests.pagination_offsetStrategy_emitsIntegerOffsetField_notCursor_andSizeReachesArtifact` |
| Decimal/fractional literal→'m' suffix | **KAPSAM DIŞI** — Java'nın decimal literal-suffix sorunu (C#'ın CS0019'u) YOKTUR; Java tarafı BigDecimal'i HER ZAMAN `new BigDecimal("...")` string-ctor ile üretir (dil-idyomu farkı, aynı güvenlik hedefi farklı mekanizmayla sağlanıyor) — bu zaten `PredicateRenderTest.decimalLiteral_usesStringCtor_preservingDecimalText` ile kapsanıyor |
| Uncharted→call-adapter+owned model | COVERED-EXISTING (fixture LegacyLedger zaten synthetic call-adapter+owned entity/type örneği) — `BoundaryEmitTest.legacyLedgerFile_isEmitted_withStubInterfaceAndClient`, `legacyLedgerEntity_isEmitted_asPlainPojo`, `unchartedEntity_isPlainPojo_notJpaEntity` |
| SourceOfTruth→cross-module FK yorumu | NEW (farklı bir entity üzerinde — fixture'da yalnız AuditLog.invoiceRef'te vardı, `JpaEmitTest` bunu zaten doğruluyordu) — `LatentConstructTests.sourceOfTruth_onDifferentEntity_emitsCrossModuleFkComment_noNavigation` |
| Note→doc-comment | NEW (ilk kez somut Javadoc metni doğrulaması — `HandlerBaseAuthThrowsConsistencyExtEmitTest.note_isRealizedExactlyOnce_*` yalnızca census-tekillik doğruluyordu, metin YOK) — `LatentConstructTests.note_emitsJavadocComment_onRequestRecord` |

### ReportTests (4 metod, C#)

| C# metod | Java karşılığı |
|---|---|
| Records_status_and_is_clean_only_when_all_realized | COVERED-EXISTING (`ReportTest.clean_allRealized_true`/`clean_unsupportedPresent_false`) + 1:1-adlı port `ReportTests.recordsStatus_andIsClean_onlyWhenAllRealized` (NEW, izlenebilirlik için) |
| Covers_matches_compound_ids_but_not_prefix_collisions | COVERED-EXISTING — `ReportTest.covers_compoundId_*`, `covers_prefixWithoutBoundarySeparator_doesNotMatch` |
| Json_is_deterministically_ordered | COVERED-EXISTING — `ReportTest.toJson_deterministic_orderIndependentOfInsertionSequence`, `toJson_rootShapeAndOrdinalEntrySort` |
| Unsupported_carries_reason_and_policy_recorded + "silentDrops ayrı JSON alanı yok" | Reason/policy kısmı COVERED-EXISTING (`ReportTest.silentDrop_usesFixedInv7ReasonConstant`, `toJson_includesReasonFieldWhenPresent`); "ayrı alan yok" iddiası NEW — `ReportTests.toJson_hasNoSeparateSilentDropsField_onlyStatusFieldOnConstructs` |

### CompletenessTests (1 metod, C#)

| C# metod | Java karşılığı |
|---|---|
| No_silent_drops_beyond_known_debt | Pozitif-yön COVERED-EXISTING (mantıksal örtüşme: boş allowlist'e göre "aşmama"≡"sıfır") — `ZeroDropTest.fixtureEmit_hasZeroSilentDrops`; ratchet-adıyla+negatif-kanıtla NEW — `CompletenessTests.noSilentDropsBeyondKnownDebtAllowlist_ratchet` + `ratchet_catchesNewUnexpectedDrop_whenManifestGainsOrphanConstruct` |

**Sonuç:** §B'nin EmitTests/LatentConstructTests/ReportTests/CompletenessTests kapsamındaki HER
maddesi ya (a) yeni bir Java testiyle doğrudan port edildi, ya (b) mevcut bir gen-spring/gen-cli
testinin zaten birebir kapsadığı gösterilerek COVERED-EXISTING işaretlendi, ya da (c) tek bir
madde (decimal 'm'-suffix) dil-idyomu farkı nedeniyle KAPSAM DIŞI bırakılıp nedeni yazıldı. Sessiz
atlama YOK.

## Acceptance sonuçları (gerçek koşum)

- **Pre-condition:** `ls $JAVA_HOME/bin/java` OK; `mvn -q -pl gen-spring -am test` (T6.2 öncesi,
  mevcut yeşil durum) exit 0 doğrulandı.
- `mvn -q -pl gen-spring -am test -Dtest='EmitTests,LatentConstructTests,CompletenessTests' -Dsurefire.failIfNoSpecifiedTests=false`
  → exit 0; `EmitTests` 8/8, `LatentConstructTests` 15/15, `CompletenessTests` 2/2 (surefire-reports
  okundu).
- `mvn -q -pl gen-core test -Dtest='ReportTests' -Dsurefire.failIfNoSpecifiedTests=false` → exit 0;
  2/2.
- **Kökten tam reactor:** `mvn -q test` → **BUILD SUCCESS, exit 0**. Gerçek surefire-reports
  toplamı: gen-core **106** (T6.1'de 104 idi, +2), gen-spring **149** (T6.1'de 124 idi, +25),
  gen-cli **7** (değişmedi), conformance **1** (değişmedi) → **toplam 263 test, 0 failure,
  0 error, 0 skipped**. Yeni test sayısı: 263 − 236 (T6.1 sonrası taban) = **27** (DoD eşiği
  ≥25 sağlandı).
- **Golden hâlâ yeşil:** `CharacterizationTest` 5/5 (T6.1'de kurulan golden `emit-snapshot.txt`
  DEĞİŞTİRİLMEDİ — `UPDATE_GOLDEN` hiç kullanılmadı; bu task emisyon davranışını DEĞİL yalnız
  testleri değiştiriyor).
- **SpringEmitter.java değişmedi:** `git status --porcelain` çalışma ağacında
  `gen-spring/src/main/java/techgen/spring/SpringEmitter.java` görünmüyor (modified listesinde
  YOK) — yalnız 4 yeni test dosyası eklendi, emisyon kodu dokunulmadı (task talimatı: "test portu,
  davranış değişmiyor").

## Self-check

1. Her §B maddesi test edildi mi ya da kapsam-dışı+neden yazıldı mı? **Evet** — yukarıdaki 4 tablo.
2. Fixture mutasyonları `with`-benzeri kopya kurucularla mı yapıldı? **Evet** —
   `LatentConstructTests` içinde `withOp`/`withEntity`/`withConsistency`/`withServing`/
   `withVisibility`/`withValidation`/`withPagination`/`withSourceOfTruth`/`withField` (record
   canonical-constructor kopyaları; gen-core model'leri immutable record olduğundan Java'da
   C#'ın `with`'i birebir yok — bu, davranışsal eşdeğeri).
3. grpc/queue→explicit Unsupported ikisi de test edildi mi? **Evet** — grpc mevcuttan (ZeroDropTest),
   queue yeni (`LatentConstructTests.serving_queue_recordedUnsupported_notSilentDrop`); ikisi de
   `silentDrops()` BOŞ kalırken UNSUPPORTED status'u doğrulanıyor.
4. @internal route bastırma normalde-exposed bir op üzerinde mi test edildi (yalnız fixture'ın
   zaten-internal op'u değil)? **Evet** — GetInvoice mutasyonu; Endpoint dosyasının HİÇ
   emit edilmediği (`Files.exists`==false) doğrulanıyor, CreateInvoice'un route'u hâlâ VAR.
5. GET param binding testi var mı? **Evet, COVERED-EXISTING** —
   `GeneratedAppCompileTest.bindAsymmetry_postRequestBody_vs_getPathVariableAndRequestParam`
   fixture'ın GetInvoice(id route + includeVoid non-route) çiftini zaten birebir doğruluyor; mükerrer
   test yaratılmadı.
6. sourceOfTruth FK yorumu test edildi mi? **Evet** — hem mevcut (`JpaEmitTest`, AuditLog.invoiceRef)
   hem yeni (`LatentConstructTests`, Invoice.customerId — farklı entity, aynı mekanizma).
7. Covers compound-id/prefix testi var mı? **Evet, COVERED-EXISTING** — `ReportTest.covers_*`
   (T2.3'te zaten 7 ayrı test).
8. KnownDebt ratchet (boş) test edildi mi? **Evet** — `CompletenessTests` 2 test (pozitif +
   ratchet'in gerçekten yakaladığını kanıtlayan negatif senaryo).
9. `mvn -q test` gerçekten koşuldu ve çıktı okundu mu? **Evet** — exit 0, surefire-reports
   dosyaları `cat`/`grep` ile açılıp gerçek sayılar bu rapora yazıldı (varsayım YOK).
10. Golden hâlâ yeşil mi? **Evet** — `CharacterizationTest` 5/5, golden dosyası bu task'ta hiç
    dokunulmadı.
11. SpringEmitter.java (emisyon kodu) değişti mi? **Hayır** — yalnız 4 yeni test dosyası eklendi.
12. Allowlist dışı dosya var mı? **Hayır** — yalnız 4 yeni test dosyası + bu rapor
    değiştirildi/eklendi (`git status --porcelain` ile doğrulandı; `.claude/settings.local.json`,
    `tasks/.pm-state/*`, `gen-cli/dependency-reduced-pom.xml` bu executor'ın DIŞINDAki
    süreçlerden/build-byproduct'lardan — hiçbiri stage edilmiyor).

## Notlar (verifier için)

1. Yeni test sayısı **27** (EmitTests 8 + LatentConstructTests 15 + CompletenessTests 2 +
   gen-core ReportTests 2), DoD eşiği (≥25) 2 test payla aşıldı.
2. Kapsam matrisi yukarıda TAM: hiçbir §B maddesi sessizce atlanmadı; 1 madde (decimal 'm'-suffix)
   açıkça dil-idyomu farkı nedeniyle kapsam dışı bırakıldı ve gerekçesi yazıldı.
3. `git status --porcelain` çalışma ağacında ayrıca `tasks/.pm-state/*` (RESUME.md, status.json,
   escalations/, subagent-prompts/, subagent-reports/, verifier-reports/) ve
   `gen-cli/dependency-reduced-pom.xml` görünüyor — bunların HİÇBİRİ bu executor'ın tool-call'larıyla
   OLUŞTURULMADI/DEĞİŞTİRİLMEDİ (paralel PM/T6.3-worktree süreçlerinden veya önceki build'lerden
   kalma); bu commit'e STAGE EDİLMEDİ.
4. `.claude/settings.local.json` stage edilmedi (standing rule).
5. SpringEmitter.java, GenConfig.java, BuildReport.java, Completeness.java gibi TÜM main-kaynak
   dosyaları bu task boyunca DOKUNULMADAN kaldı — yalnız gen-core/gen-spring test ağaçlarına
   4 yeni dosya eklendi.

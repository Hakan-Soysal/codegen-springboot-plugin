# T2.4 — Provenance IO — Executor Raporu

**Durum:** DONE
**Worktree:** `/private/tmp/claude-501/-Users-hakansoysal-Desktop-ClaudeCode-Denemeler-SpringBoot-Template/f700c184-fdb1-45e4-89de-971d3fac2909/scratchpad/wt-t2-4`
**Branch:** `t2-4-provenance`

## Yapılanlar

- `gen-spring/src/main/java/techgen/spring/ProvenanceEntry.java` — `record ProvenanceEntry(String path, String clazz, String sha256)`.
- `gen-spring/src/main/java/techgen/spring/Provenance.java` — `record Provenance(String generator, String version, List<ProvenanceEntry> files)`; `files` null→boş liste normalize.
- `gen-spring/src/main/java/techgen/spring/ProvenanceIo.java`:
  - `FILE_NAME = "provenance.json"`.
  - `write(Path outDir, Provenance)`: girdi sırasından bağımsız olarak `files`'ı `path`'e göre
    (`String.compareTo`, ordinal) sıralı bir kopya üretip `techgen.core.Json` (gen-core merkezî
    ObjectMapper) ile serialize eder; sonuna tek `\n` ekler; **atomik yazım** — aynı dizinde
    `Files.createTempFile` + `Files.move(..., ATOMIC_MOVE, REPLACE_EXISTING)` (temp aynı
    dizinde olduğu için aynı dosya sisteminde — atomik taşıma garantili); hata durumunda temp
    dosya silinir ve exception yeniden fırlatılır (sessizlik yok).
  - `tryRead(Path outDir)`: dosya yoksa veya `IOException`/`RuntimeException` (bozuk JSON,
    şema uyumsuzluğu) oluşursa `null` döner — throw yok.

## Kapsam kararları (spec'e sadık, genişletme yapılmadı)

1. **Generated-filtreleme burada YAPILMAZ.** Referans §5: "yalnız Generated listelenir
   (`DotnetEmitter.cs:191-193`)" — bu filtreleme emisyon tarafının (`DotnetEmitter`/T3.1
   `EmitWriter`) sorumluluğu; `ProvenanceIo` saf IO katmanıdır ve kendisine verilen
   `Provenance` nesnesini olduğu gibi yazar. Bunu doğrulamak için
   `write_onlyIncludesEntriesGivenByCaller_doesNotFilterItself` testi eklendi — bilerek
   `HumanSeam` etiketli bir entry yazıp okunduğunu doğruluyor. Bu, "yalnız Generated yazılır"
   kuralını ÇİĞNEMİYOR; kuralın uygulama noktasının T3.1 olduğunu netleştiriyor.
2. **sha256 hesaplama burada YOK.** T3.1 spec'i (`tasks/T3-1-emit-infra.md` §8 anti-pattern):
   "DO NOT sha256'yı dosyadan geri okuyarak hesapla — içerik string'inden" — hash'i EmitWriter
   içerik string'inden hesaplayıp `ProvenanceEntry.sha256` alanına hazır geçirecek. T2.4 kapsamı
   yalnız IO (write/tryRead) olduğundan `ProvenanceIo`'ya sha256 yardımcı metodu eklenmedi.

## Bilinen, kapsam-dışı notlar (parite/verifier için)

- Alan adı `clazz` (Java `class` reserved word olduğu için) — C# tarafında `Class`. JSON anahtarı
  `"clazz"` olarak serialize edilir. Bugün provenance.json yalnız `ProvenanceIo.tryRead` tarafından
  okunuyor (Java→Java, kendi içinde tutarlı); projenin "parite kod üzerinde, ara JSON üzerinde
  değil" prensibine göre bu bir sorun teşkil ETMEMELİ — ama conformance milestone'unda
  provenance.json'un C#-üretimi bir golden ile byte-byte karşılaştırılması PLANLANIRSA bu isim
  farkı görünür hale gelir; şimdilik böyle bir plan maddesi YOK.
- JSON formatı **compact** (indent yok) — `techgen.core.Json` merkezî mapper'ının varsayılan
  davranışı; referans dokümanda pretty-print zorunluluğu belirtilmiyor.

## Doğrulama (gerçek komut çıktısı)

```
$ mvn -pl gen-spring -am test
...
[INFO] Running techgen.spring.ProvenanceIoTest
[INFO] Tests run: 9, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.189 s
[INFO] Running techgen.spring.PlaceholderTest
[INFO] Tests run: 1, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

9 yeni test (`ProvenanceIoTest`): round-trip, trailing-newline, temp-dosya-kalmaması,
missing-file→null, corrupt-json→null, wrong-schema→null, ordinal-sıralama-determinizmi,
tekrarlı-write-determinizmi, filtreleme-yapmama.

## DoD

- [x] `mvn -q -pl gen-spring test` exit 0 (worktree'de, `-am` ile — gen-core reactor'dan derlendi)
- [x] Round-trip + bozuk-dosya-null + ordinal-sıra testleri var ve geçiyor

## Kapsam

`git diff --stat` / `git show --stat HEAD`: yalnız `gen-spring/src/main/java/techgen/spring/{Provenance,ProvenanceEntry,ProvenanceIo}.java`,
`gen-spring/src/test/java/techgen/spring/ProvenanceIoTest.java`, ve bu rapor.

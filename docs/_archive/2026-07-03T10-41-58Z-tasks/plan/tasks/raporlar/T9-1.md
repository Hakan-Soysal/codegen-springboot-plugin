# T9.1 — base-springboot-rest: capability.json + SKILL.md — Rapor

**Durum:** DONE (attempt 1/3)
**Tarih:** 2026-07-04

## Ön-koşul kontrolü (§4)

```
$ export JAVA_HOME=".../scratchpad/jdk21/jdk-21.0.11+10/Contents/Home"
$ export PATH="$JAVA_HOME/bin:$PATH"
$ mvn -q test
(exit 0 — tüm modüller yeşil; M6 davranışı kilitli, gözlemlendi: echo $? → 0)

$ java -jar gen-cli/target/gen-cli.jar fixtures/manifest.json /tmp/tg-skill-ref
emit → /tmp/tg-skill-ref  (clean=false, constructs=77, silentDrops=0)
$ ls /tmp/tg-skill-ref/src/main/java/app/billing/createinvoice/CreateInvoiceHandler.java
(dosya var)
```
Her iki ön-koşul karşılandı, işe devam edildi.

## Inputs okuma

- `SPEC.md` §1, §6.2–§6.4, §10, §12/4 — tam okundu (paket içeriği, seam mekaniği, naming, tam-açık
  bean kaydı kararı).
- `docs/referans/conformance-testler-skill-sozlesmesi.md` §C (C.1–C.6) — tam okundu (gap-protocol,
  verify-loop, archetype-playbooks, gap-registry, evals, techgen-sync mekanikleri).
- CoreTemplate1 (READ-ONLY) `plugins/codegen/skills/base-dotnet-rest/capability.json` +
  `SKILL.md` — satır satır okundu; CoreTemplate1'e hiçbir yazma yapılmadı.
- Üretilmiş örnek app (`/tmp/tg-skill-ref`, `fixtures/manifest.json`'dan emit): tüm seam dosyaları
  okundu (Handler/HandlerBase, Guards, Invariants, Trigger/TriggerBase, Consumer/ConsumerBase,
  BoundaryClient, GeneratedBootstrap/Wiring) — GERÇEK üye adları/imzalar doğrulandı (aşağıya bakınız).

## Yapılanlar (Section 5 adımları)

### 5.1 — `capability.json`
`plugins/codegen-spring/skills/base-springboot-rest/capability.json` — .NET descriptor'ının
alan-alan Java karşılığı (task §5.1 ile birebir):
- `id: "techgen-spring"`, `version: "0.1.0"`.
- `source`: repo remote'undan (`git remote -v` → `Hakan-Soysal/codegen-springboot-plugin`) türetildi;
  install komutu `.NET`'in `codegen@codegen-tools` desenine paralel `codegen-spring@codegen-spring-tools`
  aliası kullanır. **Not (T9.3 için):** bu alias/marketplace adı henüz `.claude-plugin/marketplace.json`
  ile senkron değil (o dosya T9.3'te oluşturulacak) — T9.3 uygulayıcısı bu değerle marketplace.json'u
  tutarlı kurmalı ya da farklıysa capability.json'u güncellemeli.
- `invocation`: 2-arg CLI (`manifest`, `targetDir`) — gen-cli'nin GERÇEK `Main.java` imzasıyla
  doğrulandı (3 arg değil; `.NET`'ten fark). `gen.config.json` CLI arg'ı DEĞİL — `Main.java:61`
  `manifestDir.resolve("gen.config.json")` ile manifest'in yanından otomatik okunuyor (grep ile
  doğrulandı) → task'ın öngördüğü ".NET'ten arg farkı" burada.
- `capability`: `languages:["java"]`, `architectures`, `persistence` (`postgres/sqlserver/h2/inmemory`
  — SPEC §2/§6.6 sqlite→h2 ikamesiyle tutarlı), `transports:["rest"]`, `constructsCovered` — .NET'in
  32-elemanlı listesiyle **birebir aynı sırada** kopyalandı (SPEC.md §1 prosesinde "31 construct"
  deniyor ama SPEC'in kendi parantez-içi listesi de 32 token içeriyor — SPEC'teki sayı-etiketi ile
  liste-içeriği arasında var olan, bu task'ın kapsamı dışındaki kozmetik bir tutarsızlık; **liste
  içeriği** SPEC ve .NET ile birebir, sapma yok).
- `emissionContract.seamPath` — 5 şablon, HEPSİ üretilmiş örnek app'te GERÇEK dosyalarla doğrulandı
  (bkz. §6.2 altında).
- `emissionContract.emptyStubMarker: "doldurulacak"`, `ownedTree: "gen/**"`,
  `humanTree: ["src/**","pom.xml"]`.
- `handlerSurfaceMap` — Java üye adlarıyla (`execute`, `{Op}Guards.validation{N}`/`rule{N}`,
  `{Entity}Invariants.invariant{N}`, `REQUIRED_ROLES`/`REQUIRED_SCOPES`/`OWNERSHIP`,
  `IDEMPOTENCY_KEYS`, `{Ext}Client.{op}(...)`, `{Event}To{Op}Consumer.handle(event)`,
  `{Op}{T}Trigger.start()`, `PAGINATION_STRATEGY`/`DEFAULT_PAGE_SIZE`,
  `CONSISTENCY_RISK`/`CONSISTENCY_MODE`, 6'lı `resultTaxonomy`) — HEPSİ üretilmiş dosyalardan
  BİREBİR okunarak yazıldı (uydurma yok); kanıtlar aşağıda.
- `canonicalOrder`: 9 adım .NET ile birebir aynı (idempotency→authz→validation→external-input→
  rule→entity+invariant→persist→emit→return).
- `buildReportSchema`, `gapProtocol.compliant:true`, `build` (`mvn -q -f {targetDir}/pom.xml compile`),
  `conformance` (`java -jar .../conformance.jar {appClasspath} {specsPath}`), `audit` (üç lens,
  bağımsızlık, dispozisyon, pass tanımı) — .NET'ten birebir.

### 5.2 — `SKILL.md`
`plugins/codegen-spring/skills/base-springboot-rest/SKILL.md` — .NET'in yapısı birebir, içerik
Java/Spring'e uyarlanmış:
- Frontmatter description: `[dsl-generator]` token'ı **BAŞTA** (satır 4) ve **SONDA** (satır 16);
  describe-modu cümlesi; Java-idyomlu tetikler ("seam doldur", "execute gövdesini yaz", "statik
  üretilmiş Spring projesini tamamla").
- §0 describe modu — .NET ile birebir sözleşme (yalnız `capability.json` döndür, DUR, sanctioned
  alan dışına uzanma).
- "Neyi neden böyle yapıyoruz" + Altın kurallar: `gen/**` yazma yasağı; marker-substring tespiti
  (Java marker biçimleriyle: handler/boundary/trigger/consumer/test-arrange); bilinmeyen gap DUR
  ("Yorum ≠ STOP"); codebase-grounded tek-aday istisnası (`ASSUMPTIONS.md`); tek-tip seam
  (Generation Gap: **abstract taban sınıf + human `extends`** — partial YOK); access/effect tek
  kaynağı `manifest.json` 4-anahtarlı access (writes-tripwire dahil); kapalı `Result<T>` taksonomisi
  (6 alt-tip, resultTaxonomy ile grep-eşleşir).
- Faz 0.5 (P1–P4 contract-only) + Faz 1–6 (bağlam-derleme → arketip → K1–K4 gap-gate → doldur →
  verify-loop → rapor) — mekanik detaylar `references/`'a atıflı (T9.2'nin üreteceği dosyalar).
- Arketip tablosu Java tespitiyle: Command=`*Command` record; Idempotent=`IDEMPOTENCY_KEYS` sabiti;
  Query+pagination=`PAGINATION_STRATEGY`; Trigger=`{Op}{T}TriggerBase`; Subscription=
  `{Event}To{Op}ConsumerBase`; Boundary=`{Ext}Client`; Test-arrange=
  `src/test/java/app/{scope}/{Name}Arrange.java`.
- CreateInvoice 6-faz izlenebilir akış örneği — GERÇEK üretilmiş üye adlarıyla (`tryBegin` →
  `REQUIRED_ROLES` → `CreateInvoiceGuards.validation0` → `paymentGateway.charge` → `rule0` →
  entity+`InvoiceInvariants.invariant0` → `invoiceRepository.save` → compensate/LIFO →
  `eventBus.publish` → `new Success<>(...)`).
- Referans dosya listesi (T9.2'nin üreteceği 4 dosya + capability.json).

### 5.3 — Tutarlılık denetimi (mekanik, gerçek koşum)
`/tmp/tg-skill-ref` (fixtures/manifest.json emit) + scratchpad'de üretilen ikinci bir örnek app
(`.../scratchpad/tg-skill-ref-full` — `fixtures/operations.json`'un scratchpad KOPYASINA boş
`"processes":[]`/`"flows":[]` eklenerek test-arrange yolunun da tetiklenmesi sağlandı; **gerçek
proje dosyalarına (`fixtures/`) hiçbir yazma yapılmadı**, yalnız scratchpad kopyaları kullanıldı)
üzerinde glob+grep ile her seamPath doğrulandı:

| seamPath şablonu | Gerçek eşleşen dosya |
|---|---|
| `src/main/java/app/{module}/{op}/{Op}Handler.java` | `app/billing/createinvoice/CreateInvoiceHandler.java` (+3 diğer) |
| `src/main/java/app/boundary/{Ext}Client.java` | `app/boundary/PaymentGatewayClient.java` |
| `src/main/java/app/{module}/{op}/{Op}{Trigger}Trigger.java` | `app/ops/writeauditlog/WriteAuditLogCronTrigger.java` |
| `src/main/java/app/{consumerModule}/{consumerOp}/{Event}To{Op}Consumer.java` | `app/ops/writeauditlog/InvoiceCreatedToWriteAuditLogConsumer.java` |
| `src/test/java/app/{scope}/{Name}Arrange.java` | `app/orphanop_biz.createinvoice/Biz.CreateInvoiceArrange.java` (scratchpad örneği) |

`doldurulacak` marker substring'i handler/boundary/trigger/consumer seam'lerinin TÜMÜNDE `grep -rl`
ile bulundu. (Test-arrange örneğinde bu op'un prereq'i olmadığından — CreateInvoice kendisi creator —
Arrange gövdesi boş kaldı ve marker literal'i yazılmadı; bu, "doldurulacak payload yok" durumunun
beklenen sonucu, seamPath şablonunun kendisini YANLIŞLAMAZ.)

`canonicalOrder`/`resultTaxonomy` grep-tutarlılığı Python ile programatik doğrulandı (aşağıya
bakınız, §6.2).

## Koşulan komutlar ve çıktılar (§6)

### 6.1 — JSON valid
```
$ python3 -m json.tool plugins/codegen-spring/skills/base-springboot-rest/capability.json > /dev/null
(exit 0)
```

### 6.2 — Pozitif: Step 5.3 denetimi
```
$ python3 -c "
import json
d = json.load(open('plugins/codegen-spring/skills/base-springboot-rest/capability.json'))
order = d['emissionContract']['canonicalOrder']
joined = '→'.join(order)
content = open('plugins/codegen-spring/skills/base-springboot-rest/SKILL.md').read()
print('canonicalOrder joined string present in SKILL.md:', joined in content)
tax = d['emissionContract']['handlerSurfaceMap']['resultTaxonomy']
for t in tax: print(t, '->', t in content)
"
canonicalOrder joined string present in SKILL.md: True
Success -> True
NotAuthenticated -> True
NotAuthorized -> True
NotValid -> True
NotProcessable -> True
ServerError -> True
```
Her seamPath için eşleşen gerçek dosya tablosu yukarıda (§5.3).

### 6.3 — Negatif: `.g.cs`/`partial class`/`dotnet build` yok
```
$ grep -in "\.g\.cs\|partial class\|dotnet build\|\.cs\b" plugins/codegen-spring/skills/base-springboot-rest/SKILL.md
25:(Generation Gap deseni: `.NET`'in `partial class`'ı Java'da **abstract taban sınıf + human `extends`**
```
Tek eşleşme — **karşılaştırma cümlesi** (task §6.3'ün öngördüğü istisna): Java'nın Generation Gap
mekaniğinin .NET'in `partial class`'ından FARKLI olduğunu (abstract taban + human `extends`)
açıklamak için terminoloji netliği amacıyla bilinçli tutuldu; .NET implementasyon detayı SKILL.md'ye
GÖMÜLMEDİ, yalnız "bu O DEĞİL" karşıtlığı verildi. `dotnet build`/`.g.cs` hiçbir yerde yok.

## Out-of-scope / anti-pattern kontrolü

- `references/` + `evals/` yazılmadı (T9.2 kapsamı).
- `plugin.json`/marketplace/pack yazılmadı (T9.3 kapsamı) — capability.json'daki `source.marketplace`
  alanı T9.3'ün üzerine kuracağı bir **yer tutucu tahmin** (repo remote adından türetildi); T9.3
  uygulayıcısına not edildi (yukarıda §5.1).
- Üreteç davranışı DEĞİŞTİRİLMEDİ — capability.json/SKILL.md yalnız GERÇEK üretilmiş çıktıyı
  belgeler (bkz. §5.3 kanıt tablosu); hiçbir generator/emitter kaynak dosyası dokunulmadı
  (`git status` ile doğrulandı, aşağıda).
- Alan adları .NET descriptor'ıyla birebir (seamPath/emptyStubMarker/ownedTree/humanTree/
  handlerSurfaceMap/canonicalOrder/buildReportSchema/gapProtocol/build/conformance/audit) — hiçbiri
  yeniden adlandırılmadı.
- Marker "çevrilmedi" — `doldurulacak` substring'i Java tarafında da AYNI literal (dil-bağımsız
  sözleşme, generator kaynağında da bu şekilde — `SpringEmitter.java` grep'iyle önceden görüldü).
- SKILL.md'ye üreteç-implementasyon iç detayı gömülmedi — yalnız doldurucunun görmesi gereken YÜZEY
  (üye adları/imza/marker/sıra), mekanik detaylar `references/`'a atıflı bırakıldı (T9.2).
- describe-modu §0'da, fazlardan ÖNCE — atlanmadı.

## Bulunan (ama bu task'ın kapsamında DÜZELTİLMEYEN) bağımsız gözlem

`fixtures/operations.json`'un contract-op id'si (`"biz.CreateInvoice"`) nokta içeriyor. Bu op için
gerçek TestPlan/test-arrange yolu tetiklendiğinde (scratchpad deneyinde), emitter bu id'yi doğrudan
PascalCase'leyip sınıf/paket adına gömüyor: üretilen dosya `Biz.CreateInvoiceArrange.java` içinde
`public class Biz.CreateInvoiceArrange { ... }` — **nokta içeren sınıf adı geçersiz Java söz
dizimidir** (derlenmez). Bu, `fixtures/`'ın gerçek (değiştirilmeyen) haliyle şu an tetiklenmiyor
(`processes`/`flows` `null` olduğundan test-arrange emisyonu hiç çalışmıyor) ama processes/flows
gerçek veriyle doldurulduğu an (T9.2/ileri M9 veya M-sonrası bir iterasyonda) ortaya çıkacak bir
generator defekti. **DÜZELTİLMEDİ** (task §7: "Üreteç davranışını skill'e uydurmak için DEĞİŞTİRMEK
YASAK" + bu, T9.1'in dosya-allowlist'i dışında bir gen-spring/gen-core kaynak değişikliği
gerektirir) — PM'e/ileri task'a raporlanıyor.

## `git status` — kapsam doğrulaması

```
$ git status --short
 M tasks/.pm-state/RESUME.md          <- bu task'ın ÜRÜNÜ DEĞİL, dokunulmadı, commit'e dahil edilmedi
?? .claude/settings.local.json         <- pre-existing, stage edilmedi
?? conformance/dependency-reduced-pom.xml   <- pre-existing (önceki mvn package artefaktı), stage edilmedi
?? gen-cli/dependency-reduced-pom.xml       <- pre-existing (önceki mvn package artefaktı), stage edilmedi
?? plugins/                             <- BU TASK
```

Commit'e YALNIZ:
```
A  plugins/codegen-spring/skills/base-springboot-rest/capability.json
A  plugins/codegen-spring/skills/base-springboot-rest/SKILL.md
A  tasks/raporlar/T9-1.md
```

## DoD kontrolü (Section 9)

- [x] capability.json + SKILL.md mevcut, JSON valid (`python3 -m json.tool` exit 0)
- [x] SeamPath/marker gerçek-app doğrulaması yapıldı (kanıt §5.3/§6.2 tablosunda)
- [x] `[dsl-generator]` token'ı description'da BAŞTA+SONDA; describe sözleşmesi §0'da
- [x] canonicalOrder/resultTaxonomy capability↔SKILL.md tutarlı (grep/Python doğrulaması, §6.2)
- [x] `git status`: yalnız `plugins/` (+ bu rapor) — pre-existing dirt dokunulmadan bırakıldı

## Self-check (Section 10)

1. Her seamPath'i üretilmiş app'te tek tek doğruladım mı (varsayım yok)? **Evet** — 5/5 şablon,
   gerçek dosya yolu tablosu §5.3'te; test-arrange için `fixtures/operations.json`'un gerçek hali
   `processes`/`flows`'u `null` bıraktığından (SPEC'in kendi nullable sözleşmesi), bu yolu tetiklemek
   için scratchpad'de bir KOPYA üzerinde boş dizi eklendi — **`fixtures/` projesine yazma yapılmadı**.
2. .NET capability'siyle alan-alan diff yaptım mı — eksik alan kaldı mı? **Evet** — id/version/source/
   invocation/capability/emissionContract(seamPath/emptyStubMarker/ownedTree/humanTree/
   handlerSurfaceMap/canonicalOrder/buildReportSchema)/gapProtocol/build/conformance/audit hepsi
   mevcut; eksik alan yok. Fark noktaları (invocation 2-arg vs 3-arg, persistence listesi,
   seamPath 5-eleman vs 4-eleman test-arrange dahil) SPEC/task'ın açıkça öngördüğü kasıtlı Java
   sapmaları, sessiz değil.
3. handlerSurfaceMap üye adları GERÇEK üretilen adlarla mı eşleşiyor? **Evet** — `execute`,
   `validation0`/`rule0` (`CreateInvoiceGuards.java`), `invariant0` (`InvoiceInvariants.java`),
   `REQUIRED_ROLES`/`IDEMPOTENCY_KEYS`/`THROWABLE_ERRORS` (`CreateInvoiceHandlerBase.java`),
   `PAGINATION_STRATEGY`/`DEFAULT_PAGE_SIZE` (`ListInvoicesHandlerBase.java`), `paymentGateway.
   charge`/`refund` (`PaymentGatewayClient.java`), `start()`/`handle(event)`
   (Trigger/Consumer seam'leri) — hepsi `/tmp/tg-skill-ref` çıktısından okunarak yazıldı.
4. describe-modu metni "yalnız capability döndür + DUR" diyor mu? **Evet** — SKILL.md §0, üç adım
   (yalnızca oku+döndür / DUR / sanctioned-alan sınırı).
5. Allowlist dışı dosya var mı? **Hayır** — `git status --porcelain` ile doğrulandı; pre-existing
   dirt (`tasks/.pm-state/RESUME.md`, `.claude/settings.local.json`,
   `conformance|gen-cli/dependency-reduced-pom.xml`) dokunulmadan bırakıldı, stage edilmedi.

# T4.5 Raporu — Kalan dispositionlar: saga/calls/host/sourceOfTruth-policy/visibility/serving/module-ext süpürmesi (M4 kapanış)

**Durum:** DONE · **Tarih:** 2026-07-03 · **Attempt:** 1/3

## Yapılanlar

Tamamı `SpringEmitter.java` (gen-spring) üzerinde; referans CoreTemplate1 `DotnetEmitter.cs`
(READ-ONLY okundu: `:70-165` emit sırası, `:789-824` HostFile, `:912-955` BoundaryFile
(calls/compensate), `:1324-1331` `ExtComment` genel yorum-helper'ı, `:1455-1493` `OperationFile`
param-ext) Java/Generation-Gap'e uyarlandı.

### Step 5.1 — calls + compensate (saga)

Yeni `callsAndCompensateComments(systemName, kind, callEdges, report)` helper'ı — hem
`boundaryFile` (external) hem `unchartedFile` (uncharted) sonunda çağrılır; `kind` + `to.system`
eşleşen her callEdge için trailing yorum `// calls: {from} -> {to.system}.{to.op} ({kind})` +
`realized("calls", from)`; compensate'li edge AYRICA `// saga: compensate = {system}.{op}
(ters-sıra/LIFO — doldurucu playbook'u)` + `realized("compensate", from)` +
`policy("saga-orchestration-state", "in-memory (generator-policy)")`. Fixture: `CreateInvoice ->
PaymentGateway.charge` (compensate=refund, `PaymentGateway.java` sonunda) + `WriteAuditLog ->
LegacyLedger.post` (compensate yok, `LegacyLedger.java` sonunda).

### Step 5.2 — Host/deployables

Yeni `gen/java/app/DeploymentTopology.java` (yalnız `deployables>0`; `deploymentTopologyJava`)
— `public static final Map<String, List<String>> DEPLOYABLES` **statik `LinkedHashMap` +
`static { DEPLOYABLES.put(...); ... }`** (Map.of KULLANILMADI — kaynak-metin/insertion sırası
determinizmi, anti-pattern §8 uyarısı birebir izlendi); deployable-ext → `typeLevelExtComment`
(yorum + `realized("@{ns}.{name}", name)` + jenerik `{ns}-realization` policy) + her deployable
`realized("deployable", name)` + bir kez `policy("deployment-topology", "modular-monolith host:
units single-process co-hosted (generator-policy)")`. Fixture: `BillingService`(units=[Billing]),
`OpsService`(units=[Ops], ext=`@deploy.region`).

### Step 5.3 — visibility + serving süpürmesi

- `requestRecordJava`'ya `report.policy("visibility", "internal".equals(visibility) ?
  "internal-no-route" : "exposed-route (generator-policy)")` eklendi (`realized("visibility",...)`
  zaten T3.6'dan vardı; policy YENİ).
- Ana op döngüsünde (Step 5.4, mevcut Endpoint bloğu) serving işleme **dosya emisyonundan
  bağımsız** hale getirildi: ÖNCE (varsa) Endpoint dosyası internal/rest kuralına göre yazılır;
  SONRA `op.op().serving()` TAMAMI ayrı döngüde gezilir — `rest` → `realized("serving",
  "{op}:rest")`; non-rest (grpc/queue/...) → `report.unsupported("serving", "{op}:{proto}",
  "REST-only binding")` + `policy("serving-{proto}", "unsupported: REST-only binding
  (generator-policy)")`. **Önceki kod** `realized("serving", opId+":rest")` çağrısını yalnız
  `emitEndpoint` (visibility≠internal ∧ hasRest) bloğunun İÇİNDE yapıyordu — internal+rest
  kombinasyonunda (fixture'da yok ama şema-genel risk) silentDrop üretebilirdi; artık serving
  kapsama dosyanın emisyon kararından ayrıştırıldı. Fixture canlı örnek: `CreateInvoice` hem
  `@rest POST /invoices` (realized, Endpoint dosyasına gider) HEM `@grpc()` (UNSUPPORTED, drop
  DEĞİL) taşıyor.

### Step 5.4 — module-ext + param-ext (op/boundary-op/entity-field/type-field/event-payload)

- **`typeLevelExtComment`** genelleştirildi (indent parametreli overload eklendi; referans
  `ExtComment(..., indent="")` paritesi) — davranışı DEĞİŞMEDİ, yalnız 3 yeni çağrı-sitesinde
  (module/boundary-op-param/entity-field) tekrar kullanılabilir hale geldi.
- Yeni `gen/java/app/{module}/ModulePrelude.java` (`modulePreludeJava`; yalnız `module.ext()>0`)
  — module-level ext yorumu + `realized`/`{ns}-realization` policy. Fixture: `Ops` modülü
  (`@audit.module`); `Billing` modülünde ext yok → dosya YOK.
- **op signature param-ext** (`requestRecordJava`): her param için ext varsa
  `// {ParamPascal}: ext: @ns.name (realizasyon = policy)` satırı (record'dan önce) +
  `realized`/`{ns}-realization`. Fixture: `WriteAuditLog.seq` (`@sensitivity.tag`).
- **boundary-op param-ext** (`boundaryFile` + `unchartedFile`, ortak desen): her boundary-op
  parametresi için aynı yorum-formu, owner `"{ext|u}.{op}.{param}"`. Fixture:
  `PaymentGateway.charge.amount` (`@sensitivity.tag`) + `LegacyLedger.post.amount`
  (`@ucparam.tag`) — **T4.4 raporunun kendi notlar bölümünde açıkça bıraktığı boşluk** (bkz.
  `tasks/raporlar/T4-4.md` Not #1) burada kapatıldı.
- **entity field ext** (`entityJava`): `typeLevelExtComment(f.ext(), "{entityId}.{field}",
  report, "    ")` — 4-boşluk girinti, alan bildirimden önce. Fixture: `Invoice.customerId`
  (`@crypto.encrypted` + `@sensitivity.tag`, ÇOKLU ext aynı alanda), `AuditLog.note`
  (`@sensitivity.tag`).
- **type field ext** (`typeJava`, composite kind): `AuditMeta.source` (`@sensitivity.tag`).
- **event payload field ext** (`eventJava`): `InvoiceCreated.invoiceId` (`@sensitivity.tag`).

## Testler — `ZeroDropTest.java` (yeni, 4 test)

1. `fixtureEmit_hasZeroSilentDrops` — fixture emit + `Completeness.check` →
   `report.silentDrops()` **BOŞ** (assert mesajı drop listesini `construct / id` formatında basar
   — teşhis edilebilirlik).
2. `fixtureEmit_hasAllRequiredPolicyKeys` — task Step 5.5'te sayılan 21 policy anahtarının
   TAMAMI `report.policies()` içinde: `deployment-topology, saga-orchestration-state,
   consistency-mode, dedup-store, pagination-strategy, cursor-token, trigger-wiring,
   http-binding, guard-linkage, source-of-truth, uncharted-realization, visibility,
   serving-grpc, audit-realization, metric-realization, trigger-realization, http-realization,
   sensitivity-realization, crypto-realization, schema-realization, deploy-realization,
   ucparam-realization`.
3. `grpcServing_isUnsupported_notSilentDrop` — `("serving","CreateInvoice:grpc")` status ==
   `UNSUPPORTED` (pozitif) + status ≠ `SILENT_DROP` (negatif).
4. `cleanIsFalse_dueToUnsupported_butSilentDropsEmpty` — **Clean≠exit ayrımının kanıtı**:
   `report.clean()==false` (grpc unsupported entry'si var) AMA `report.silentDrops().isEmpty()`
   (INV-7 unsupported≠drop felsefesi).

## Acceptance sonuçları (gerçek koşum)

- Precondition: JDK21 `ls $JAVA_HOME/bin/java` OK; `mvn -q -pl gen-spring -am test` (T4.4 sonrası
  temel) **exit 0** (değişiklik öncesi doğrulandı).
- Değişiklik sonrası `mvn -q -pl gen-spring -am compile` → **exit 0**.
- `mvn -pl gen-spring -am test -Dtest=ZeroDropTest -Dsurefire.failIfNoSpecifiedTests=false` →
  **Tests run: 4, Failures: 0, Errors: 0, Skipped: 0** (ilk denemede yeşil — silentDrop=0 GERÇEK
  koşumda okundu).
- `mvn -pl gen-spring -am test` (TAM reactor, tüm mevcut + yeni testler) →
  - gen-core: **104 test, 0 failure, 0 error**.
  - gen-spring: **119 test, 0 failure, 0 error** (önceki 115 + yeni `ZeroDropTest` 4).
  - `BUILD SUCCESS`.
  - `BoundaryEmitTest` (13, `generatedApp_realMvnCompile_exitsZero_afterT44` dahil) ve
    `GeneratedAppCompileTest` (7, `generatedApp_realMvnCompile_exitsZero` dahil) — HER İKİ
    **gerçek `mvn compile`** e2e testi de yeni emisyonlarla (DeploymentTopology.java,
    ModulePrelude.java, güncellenmiş Boundary/Uncharted/request-record dosyaları) **exit 0**
    geçti; **hiçbir mevcut test dosyasında değişiklik gerekmedi** (ctor imzası/parametre sayısı
    değişmedi — bu task yalnız yorum + report çağrısı ekledi, DI/imza dokunulmadı).
- Manuel doğrulama (gerçek emit çıktısı, `git diff` DIŞI `/tmp` geçici dizine — sonradan
  silindi): `DeploymentTopology.java`, `app/ops/ModulePrelude.java`,
  `boundary/PaymentGateway.java` (calls+saga+param-ext yorumu), `uncharted/LegacyLedger.java`
  (calls yorumu, compensate YOK — doğru), `billing/Invoice.java` (customerId çoklu-ext yorumu),
  `ops/writeauditlog/WriteAuditLogCommand.java` (seq param-ext yorumu) tek tek okunarak
  gözle doğrulandı — beklenen şekille birebir.

## Notlar (verifier için)

1. **silentDrop sayısı: 0** (task DoD birincil şartı) — `ZeroDropTest.fixtureEmit_hasZeroSilentDrops`
   ile GERÇEK koşumda doğrulandı.
2. **Boundary param-ext (koordinasyon notunda ZORUNLU işaretlenen 2 çift) REALİZE EDİLDİ:**
   `(@sensitivity.tag, PaymentGateway.charge.amount)` ve `(@ucparam.tag,
   LegacyLedger.post.amount)` — kapsam task Step 5.4'te istenen 5 param-ext yeri (op signature +
   boundary + event payload + entity field + type field) TAMAMI implemente edildi.
3. **grpc unsupported:** `CreateInvoice @grpc()` → `report.unsupported("serving",
   "CreateInvoice:grpc", "REST-only binding")` + `policy("serving-grpc", "unsupported:
   REST-only binding (generator-policy)")`; Endpoint dosyası yalnız REST route'unu mapler (grpc
   mapleme YOK — .NET paritesi, transport binding henüz eklenmedi).
4. **Serving-realize/dosya-emisyonu ayrıştırması** dışında hiçbir mevcut DAVRANIŞ değişmedi
   (ctor imzaları, bean kayıtları, dosya adları/konumları AYNI) — bu yüzden T4.1-T4.4'ün hiçbir
   testi güncellenmedi (T4.4 raporundaki gibi bir ctor-senkron zinciri bu task'ta YOK, çünkü hiçbir
   HandlerBase ctor parametresi eklenmedi — standing rule #8'e bilinçli uyum: "muhtemelen ctor'a
   param EKLEMİYORSUN" doğrulandı).
5. **Component-scan açılmadı**; yeni dosyalar (`DeploymentTopology`, `ModulePrelude`) bean/DI
   İÇERMEZ — saf statik/yorum artefaktları (referans `.NET Host.g.cs`/`Module.g.cs` paritesi,
   ikisi de DI'a kayıtlı DEĞİL).
6. Golden snapshot'a DOKUNULMADI (T6.1 kapsamı); exit-code sözleşmesinin kendisi (T5.1) veya
   studyo fixture 0-drop kanıtı (T6.3) bu task'ın DIŞINDA bırakıldı (task §7 out-of-scope birebir).
7. CoreTemplate1: yalnız `DotnetEmitter.cs` okundu (`:70-165`, `:789-824`, `:912-1071`,
   `:1324-1331`, `:1440-1493`), hiçbir yazma yapılmadı.
8. Kapsam dışı dosyalara dokunulmadı — `git diff --stat`: yalnız `SpringEmitter.java` (main) +
   yeni `ZeroDropTest.java` (test). `tasks/.pm-state/`, `.claude/settings.local.json` bu commit'e
   dahil edilmedi.

# T3.5 — JavaPredicateRenderer + Guards + Invariants (rapor)

**Durum:** DONE · main (worktree yok) · `mvn -pl gen-spring -am test` → BUILD SUCCESS
(gen-core 104, gen-spring 56 test; 0 failure/error).

## Yapılanlar

### 5.1 JavaPredicateRenderer (yeni dosya)
`gen-spring/.../JavaPredicateRenderer.java` — `ExprWalk.Render<Frag>` implementasyonu.
Dil-nötr yürüyüş gen-core `ExprWalk`'ta kaldı; Java render stratejisi burada (anti-pattern §8).
- `render(root, resolveType)` → Guards biçimi (`input.{prop}()`); `render(root, resolveType, false)`
  → Invariants biçimi (bare parametre adı). Döner: `(String expr, List<List<String>> paths)`.
- Karşılaştırma tip-duyarlı:
  - Decimal bağlam → `(L.compareTo(R) {op} 0)` (`=`→`==`); Decimal literal `new BigDecimal("{v}")`
    (String-ctor ZORUNLU — temsil hatası önlenir).
  - String eşitlik → `(L.equals(R))`, eşitsizlik `!=` → `(!L.equals(R))`.
  - primitif (Int/Double/Bool) → doğal operatör (`=`→`==`).
  - arith Decimal bağlamında `.add/.subtract/.multiply/.divide`; primitif doğal.
  - and→`&&`, or→`||`; her binary parantezli (ExprWalk sözleşmesi).
- Tip çözümü: resolveType → `ExprWalk.inferLiteralTypes` → son-çare (aşağıdaki nota bak).
- `javaFieldType(path, resolveType, hints)`: render kararıyla **aynı** nötr tipten Java tipi
  (BigDecimal alan ↔ compareTo, primitif alan ↔ doğal operatör → derlenebilirlik garantisi).

### 5.2 Guards emisyonu (SpringEmitter)
`guardsJava(op, gm, report)` — validation/rule/permit varsa `gen/java/app/{module}/{op}/{Op}Guards.java`.
`public static boolean validation{i}({Op}Validation{i}Input input)` + nested `public record` input'ları
(distinct path → tipli alan). guardRef → `// guardRef: {id}` yorumu + `guard-linkage` policy +
`realized("guardRef", opId)`. `realized("validation"|"rule"|"permit", opId)` op-başına bir kez
(Completeness census owner id'leriyle birebir). Renderer `UnsupportedConstruct` atarsa
`report.unsupported(...)` + throw-stub metot (sessizlik yok).

### 5.3 Invariants emisyonu (SpringEmitter)
`invariantsJava(entity, gm, report)` — invariants varsa `{Entity}Invariants.java`.
`public static boolean invariant{i}({tip} {alan})` tipli **bare parametre** biçimi (input record YOK,
task §5.3). `realized("invariant", entityId)` (+guardRef varsa).

### 5.4 Testler (yeni)
- `PredicateRenderTest` (9 test): compareTo HEM literal HEM path-path; Int doğal operatör (compareTo
  SAPMASI negatif); String equals + `!=` negatif; and/or parantezleme + distinct path bir kez;
  Decimal literal String-ctor; agg node + op'suz cmp → `UnsupportedConstruct`.
- `GuardsEmitTest` (5 test): CreateInvoiceGuards (validation0/rule0 + iki BigDecimal input record);
  WriteAuditLogGuards (Int validation0 + guardRef yorumu, String permit0 equals); Invoice/AuditLog
  Invariants; build-report realized+policy; **javac** Guards+Invariants+Result ailesi → 0 diagnostic.

## SPEC/task iç-tutarsızlığı — son-çare tipi (RAPORLANIR)

Task §5.1 metni son-çare tip için "Double varsay" diyor; ANCAK task §5.4 aynı fixture için
"input record'da BigDecimal amount, **BigDecimal resourceCreditLimit**" bekliyor ve §5.1 parantezi
"(parite: .NET aynı sıra)" diyor. Fixture'da `CreateInvoice.rule0 = amount <= resource.creditLimit`;
`resource.creditLimit` write-hedefi Invoice'ta **creditLimit alanı olmadığından** TypeEnv ile
çözülemez → son-çareye düşer. Son-çare **Double** olsaydı `compareTo(input.resourceCreditLimit())`
bir `double` alanına uygulanır ve **DERLENMEZDİ** (INV-4 ihlali); ayrıca §5.4 beklentisiyle çelişirdi.
CoreTemplate1 `ResolveType` son-çaresi `decimal`'dir. Bu üç kanıt (task §5.4 beklentisi + .NET paritesi
+ derlenebilirlik) doğrultusunda son-çare **Decimal (BigDecimal)** alındı; "Double" ifadesi typo kabul
edildi. Kod yorumunda ve bu raporda belgelendi. Golden GÜNCELLENMEDİ (bu task golden'a dokunmaz).

## Kapsam
`git diff --stat`: yalnız `gen-spring/` (SpringEmitter + 3 yeni dosya) + bu rapor. `.pm-state/`,
`.claude/settings.local.json` stage EDİLMEDİ. Push YOK.

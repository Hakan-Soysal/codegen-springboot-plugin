# T6.1 Raporu — Golden snapshot + characterization + byte-determinizm

**Durum:** DONE · **Tarih:** 2026-07-04 · **Attempt:** 1/3

## Yapılanlar

Referans CoreTemplate1 `tests/Gen.Tests/CharacterizationTests.cs` + `golden/emit-snapshot.txt`
(READ-ONLY okundu; biçim/mekanik birebir taşındı, Java hedefine uyarlandı) + SPEC §9 + referans
`gen-dotnet-emisyon-sozlesmesi.md` §8 + `conformance-testler-skill-sozlesmesi.md` §B baz alındı.

### Step 5.1 — `gen-spring/src/test/java/techgen/spring/GoldenSupport.java` (yeni)

- `snapshot(Path root)`: emit ağacını `Files.walk` ile gezer; `provenance.json` ve
  `build-report.json` (`METADATA` set'i) HARİÇ tutar; her dosya için `relPath.replace('\\','/') +
  "\t" + sha256hex(bytes) + "\n"`; `Comparator.naturalOrder()` (ordinal/`String::compareTo`) ile
  sıralar.
- `compareOrUpdate(String actual)`: `UPDATE_GOLDEN=1` env VEYA golden dosyası yoksa doğrudan
  `actual`'ı `src/test/resources/golden/emit-snapshot.txt`'e yazar (`null` döner — golden ELLE asla
  yazılmadı, yalnız bu mekanizma üzerinden); aksi halde satır-bazlı diff (YENİ/DEĞİŞEN vs
  KAYIP/ESKİ, dosya adı görünür) mesajı üretir.

### Step 5.2 — `gen-spring/src/test/java/techgen/spring/CharacterizationTest.java` (yeni, 5 test)

1. `emit_tree_matches_golden_snapshot` — fixture emit → `GoldenSupport.snapshot` == golden;
   `compareOrUpdate` `null` dönmezse `fail(diff)`.
2. `unchanged_input_does_not_rewrite_generated_file` — `gen/java/app/Result.java` mtime'ı
   `Files.setLastModifiedTime` ile `2001-01-01T00:00:00Z`'ye çekilir (Thread.sleep YOK); aynı gm
   ile ikinci emit → mtime AYNI kaldı (write-only-if-changed).
3. `removed_operation_prunes_generated_but_keeps_human_logic` — ilk emit sonrası
   `src/main/java/app/billing/getinvoice/GetInvoiceHandler.java`'a elle `"// HUMAN BODY — elle
   yazılmış iş mantığı\n"` yazılır; `GenerationModel` in-memory'de `GetInvoice` operasyonu
   filtrelenerek yeniden emit edilir → `gen/java/app/billing/getinvoice/` dizini YOK (prune) +
   human handler dosyası VAR + İÇERİĞİ birebir korunmuş (yalnız varlık değil, içerik de assert'li
   — anti-pattern §8 uyarısı).
4. `human_shell_survives_regeneration` — `pom.xml` / `Application.java` / `application.yml`
   elle bozulur, ikinci emit sonrası bozulmuş içerikler DURUYOR; `gen/parent/pom.xml` üreteç-sahibi
   içerikle (`AUTO-GENERATED by techgen-spring` marker'ı) yeniden üretilmiş durumda.
5. `generated_tree_is_byte_deterministic` — **iki AYRI** `@TempDir` (`dirA`, `dirB`) parametresi
   ile aynı gm iki farklı dizine emit edilir → iki snapshot string'i tam eşit.

### Step 5.3 — Golden üretimi

`UPDATE_GOLDEN=1 mvn -pl gen-spring -am test -Dtest=CharacterizationTest#emit_tree_matches_golden_snapshot
-Dsurefire.failIfNoSpecifiedTests=false` ile üretildi (ELLE YAZILMADI).

## Golden içerik denetimi

- **Satır sayısı: 63.**
- `grep -i "build-report\|provenance"` → **0 eşleşme** (dosya açılıp gözle de doğrulandı).
- İçerdikleri (doğrulandı): `pom.xml`, `gen/parent/pom.xml`, `src/main/java/app/Application.java`,
  4 op slice'ı (`createinvoice`, `getinvoice`, `listinvoices`, `writeauditlog` — hem `gen/`
  hem `src/` tarafı), `boundary/PaymentGatewayClient.java`, `uncharted/LegacyLedger*.java`.
- **Sapma notu (§4 gereği raporlanıyor, çözülmedi):** Task Step 5.3 metni "test iskeleti
  dosyaları VAR" diyor; mevcut `SpringEmitter` henüz `gm.testPlan()`'dan hiçbir test-iskeleti
  dosyası ÜRETMİYOR (kod tabanında `testPlan`/`ProcessTest`/`ScenarioTest` kullanan tek yer bu
  task'ın kendi `CharacterizationTest.java`'sı — emitter'da yok). Bu, task betiminin ileri bir
  M6/M7 emitter özelliğini varsaymasından kaynaklanan bir spec-vs-mevcut-implementasyon
  tutarsızlığı; T6.1 kapsamı SADECE golden mekanizmasını mevcut emit çıktısına karşı kurmak
  olduğundan, bu tutarsızlık ÇÖZÜLMEDİ (emitter'a özellik eklenmedi — T6.1 dışı) ve burada
  bilgilendirme amaçlı raporlanıyor.

## Acceptance sonuçları (gerçek koşum)

- **Pre-condition:** `ls $JAVA_HOME/bin/java` OK; `mvn -q test` (kökten, T6.1 öncesi) exit 0;
  `java -jar gen-cli/target/gen-cli.jar fixtures/manifest.json /tmp/tg-golden-pre` → `emit → ...
  (clean=false, constructs=77, silentDrops=0)` + `OK` (exit 0).
- **6.1** — `mvn -q test` (kökten, TÜM modüller) → **BUILD SUCCESS, exit 0**. Gerçek reactor
  test sayıları: gen-core **104**, gen-spring **124** (yeni `CharacterizationTest` 5 dahil),
  gen-cli **7**, conformance **1** → **toplam 236 test, 0 failure, 0 error**.
- **6.2** — Golden dosyası stage için hazır (`gen-spring/src/test/resources/golden/emit-snapshot.txt`,
  63 satır); içerik denetimi yukarıda.
- **6.3 — Kasıtlı-kırma/geri-alma döngüsü (GERÇEKTEN yapıldı):**
  1. `SpringEmitter.resultInterfaceJava()` şablonuna kasıtlı bir boş satır eklendi.
  2. `mvn -pl gen-spring -am test -Dtest=CharacterizationTest#emit_tree_matches_golden_snapshot`
     → **KIRMIZI**: `Tests run: 1, Failures: 1`; diff mesajında dosya adı açıkça göründü:
     `YENİ/DEĞİŞEN: gen/java/app/Result.java <yeni-sha>` / `KAYIP/ESKİ: gen/java/app/Result.java
     <eski-sha>`.
  3. Değişiklik `Edit` ile geri alındı; `git diff --stat -- .../SpringEmitter.java` → BOŞ (dosya
     birebir orijinaline döndü).
  4. Aynı test tekrar çalıştırıldı → **YEŞİL** (exit 0, hata yok).
  5. Kökten `mvn -q test` tekrar çalıştırıldı → **BUILD SUCCESS, exit 0** (final durum).

## Self-check

1. Golden'ın build-report/provenance İÇERMEDİĞİNİ dosyayı açıp doğruladım mı? **Evet** (`grep -i
   "build-report\|provenance"` → 0 eşleşme; ayrıca `GoldenSupport.METADATA` set'i her iki adı da
   filtreler).
2. Kasıtlı-kırma testini gerçekten yaptım mı (kırmızıyı gözümle gördüm mü)? **Evet** — yukarıdaki
   6.3 log'u gerçek `mvn` çıktısından.
3. Byte-determinizm testi iki AYRI dizin mi kullanıyor? **Evet** — `@TempDir Path dirA, @TempDir
   Path dirB` iki bağımsız JUnit temp dizini (aynı dizine iki emit DEĞİL).
4. mtime testinde flake riski bıraktım mı? **Hayır** — `Files.setLastModifiedTime` ile deterministik
   geçmiş zaman damgası; `Thread.sleep` kullanılmadı.
5. Allowlist dışı dosya var mı? **Hayır** — yalnız `GoldenSupport.java`, `CharacterizationTest.java`,
   `golden/emit-snapshot.txt` (+ bu rapor) staged/değiştirildi. `SpringEmitter.java` kasıtlı-kırma
   turunda geçici değiştirilip birebir geri alındı — net diff YOK, commit'e girmiyor.

## Notlar (verifier için)

1. `git status` çalışma ağacında ayrıca `tasks/.pm-state/RESUME.md` (bu executor'ın DIŞINDAki bir
   süreçten, hiçbir tool-call ile dokunulmadı) ve `gen-cli/dependency-reduced-pom.xml` (shade-plugin
   build byproduct, önceki task'lardan kalma) görünüyor — İKİSİ DE bu commit'e STAGE EDİLMEDİ
   (allowlist dışı).
2. `.claude/settings.local.json` stage edilmedi (standing rule).
3. Golden konumu: `gen-spring/src/test/resources/golden/emit-snapshot.txt` (63 satır,
   `relpath\tsha256`, `/` ayraçlı, ordinal sıralı).

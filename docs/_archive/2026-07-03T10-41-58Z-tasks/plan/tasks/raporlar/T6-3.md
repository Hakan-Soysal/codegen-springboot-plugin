# T6.3 Raporu — E2E compile + studyo ölçek smoke

**Durum:** ACCEPTANCE_FAILED · **Tarih:** 2026-07-04 · **Attempt:** 1/3

## Yapılanlar

Plan §M6 T6.3 + §0 worktree kopyasından okundu. Mevcut e2e desen (`GeneratedAppCompileTest`,
T3.6'dan) incelendi: gerçek `mvn -q -f <pom> compile` alt-süreciyle ProcessBuilder üzerinden
üretilen app'i derleyen desen zaten mevcut ve `@Tag("e2e")` ile işaretli — bu, task metnindeki
"(1) fixture emit → mvn -q compile exit 0" testini KARŞILIYOR (yeniden yazılmadı; T6.3 kapsamı
yalnız yeni (2) testi eklemek).

### Yeni dosya — `gen-spring/src/test/java/techgen/spring/StudyoScaleE2ETest.java`

`ZeroDropTest` (Completeness.check + silentDrops() deseni) ile `GeneratedAppCompileTest`
(ProcessBuilder + gerçek `mvn compile`) desenlerini birleştiren tek `@Tag("e2e")` test:

1. `fixtures/studyo.manifest.json` (+ `studyo.operations.json`, linked contract) yüklenir,
   `GmBuilder.build` ile GM kurulur, `SpringEmitter.emit` ile `@TempDir`'e emit edilir (h2/inmemory
   config).
2. `Completeness.check(manifest, report)` çalıştırılır; `report.silentDrops()` BOŞ olmalı (INV-7).
3. Emit süresi (`System.nanoTime` farkı) + üretilen dosya sayısı (`Files.walk` + regular-file
   count) `System.out.println` ile raporlanır.
4. Gerçek `mvn -q -f <pom> compile` alt-süreci çalıştırılır; exit code 0 beklenir.

## Acceptance sonuçları (GERÇEK koşum)

- **Pre-condition:** `ls $JAVA_HOME/bin/java` OK. `mvn -q -pl gen-spring -am test` (worktree,
  T6.3 öncesi, fresh worktree HEAD=13b83e1) → **exit 0** (tüm mevcut testler yeşil).
- **Test (1) — `GeneratedAppCompileTest.generatedApp_realMvnCompile_exitsZero`**: mevcut,
  reactor koşumunda **YEŞİL** (invoice fixture → gerçek `mvn compile` exit 0).
- **Test (2) — `StudyoScaleE2ETest.studyoManifest_emitsWithZeroSilentDrop_andRealMvnCompileExitsZero`**:
  **KIRMIZI**. Ayrıntı:
  - `silentDrops()` assertion'ı **GEÇTİ** → studyo fixture'ında INV-7 açısından **silentDrop=0**
    (emisyon/completeness tarafı sağlam).
  - Emit metrikleri (gerçek koşumdan, izole `-Dtest=StudyoScaleE2ETest` çalıştırmasında konsola
    yazıldı): **emit süresi = 148ms**, **üretilen dosya sayısı = 239**.
  - Gerçek `mvn -f <pom> compile` adımı **exit 1** ile FAIL etti — aşağıdaki gerçek derleme
    hatalarıyla:

### Bulunan derleme hataları (gerçek `javac` çıktısı, üç ayrı kök neden)

1. **Cross-package `Unit` importu eksik.** `gen/java/app/Unit.java` kök `app` paketinde
   tanımlı; `Result<Unit>` dönen sorgu/komut handler'ları (`app.studio.yeracildibildir.*`,
   `app.notifications.pushslotopened.*`, `app.boundary.PushService(Client)`, vb. — studyo'da
   Unit-dönüşlü çok sayıda operasyon var) `Unit`'i import ETMİYOR → `cannot find symbol: class
   Unit`. Invoice fixture'ında hiçbir operasyon `Unit` dönmediği için bu yol hiç egzersiz
   edilmemiş (T3.6/T4.x testlerinde yakalanmamış).
2. **Cross-package enum importu eksik.** `app.shared.SessionStatus`, `app.shared.AppointmentStatus`
   gibi paylaşılan enum'lar başka modül paketlerindeki entity'lerde (`app.studio.Session`,
   `app.studio.Appointment`, ...) alan tipi olarak kullanılıyor ama import edilmiyor → `cannot
   find symbol`. Invoice fixture'ında cross-module enum-tipli alan senaryosu yok.
3. **`PackageInvariants` — Comparable (non-primitif) tip için geçersiz operatör.** Üretilen
   `invariant0(LocalDate endsAt, LocalDate startsAt) { return (endsAt >= startsAt); }` — Java'da
   `>=` yalnız primitif sayısal tipler için geçerli; `LocalDate` için derlenmiyor
   (`bad operand types for binary operator '>='`). Predicate renderer yalnız primitif
   karşılaştırmaları destekliyor gibi görünüyor; `LocalDate`/`Comparable` alan çiftli invariant'lar
   invoice fixture'ında yok.

**Bu üç bulgu SpringEmitter'da GERÇEK, tekrar üretilebilir kod-üretim hatasıdır** (fixture/test
hatası DEĞİL — emisyon manuel olarak `ScratchEmitStudyoTest` ile ayrıca doğrulandı, aynı üç hata
gözlendi). Görev talimatı gereği **SpringEmitter DEĞİŞTİRİLMEDİ** (emisyon kapsam dışı — bu
executor yalnız yeni bir `@Tag("e2e")` test dosyası ekleyebilir). Bu nedenle T6.3 DoD'nin "iki test
yeşil" maddesi bu attempt'te KARŞILANAMADI; bulgu, ayrı bir bug-fix task'ı (SpringEmitter: (a)
cross-package `Unit`/enum import toplama, (b) Comparable tip invariant render'ı `compareTo`/
`isBefore`/`isAfter` kullanacak şekilde düzeltme) olarak PM'e iletilmelidir.

## Out-of-scope kontrolü

`git status --short` (worktree) → yalnız `gen-spring/src/test/java/techgen/spring/StudyoScaleE2ETest.java`
(untracked, yeni). `git diff --stat` → boş (mevcut hiçbir dosya değiştirilmedi). Geçici teşhis
dosyası `ScratchEmitStudyoTest.java` oluşturulup emisyonu tekrar üretmek için kullanıldı, teşhis
sonrası **silindi** — commit'e girmiyor.

## Self-check

1. Pre-condition (`ls java`, `mvn -q -pl gen-spring -am test`) gerçekten koşuldu mu ve exit 0
   gözlendi mi? **Evet.**
2. İki test de gerçekten `mvn` alt-süreciyle koşuldu mu (yalnız derleme/syntax check değil)?
   **Evet** — her ikisi de `ProcessBuilder` ile gerçek `mvn -q -f <pom> compile` çalıştırıyor;
   çıktı okunup assert ediliyor.
3. SpringEmitter'a dokunuldu mu? **Hayır** — `git diff --stat` boş; yalnız teşhis amaçlı geçici
   test dosyası oluşturulup silindi (kalıcı değişiklik yok).
4. Studyo emit süresi + dosya sayısı gerçek koşumdan mı okundu? **Evet** — izole
   `-Dtest=StudyoScaleE2ETest` koşumunun konsol çıktısından: `148ms`, `239 dosya`.
5. silentDrop=0 gerçekten doğrulandı mı? **Evet** — `Completeness.check` + `silentDrops().isEmpty()`
   assertion'ı bu koşumda GEÇTİ (derleme hatasından ÖNCE gelen adım).
6. "İki test yeşil" DoD maddesi karşılandı mı? **HAYIR** — (1) yeşil, (2) kırmızı (gerçek emitter
   bug'ı nedeniyle, test/fixture hatası değil).

## Notlar (verifier için)

- Studyo emit süresi: **148 ms**. Üretilen dosya sayısı: **239**. silentDrop: **0**.
- Reactor test sayıları (bu attempt'in son tam koşumu, `mvn -q -pl gen-spring -am test`):
  **Tests run: 125, Failures: 1, Errors: 0, Skipped: 0** (124 önceki + 1 yeni `StudyoScaleE2ETest`;
  tek failure budur — `GeneratedAppCompileTest` dahil diğer 124 test YEŞİL).
- Worktree: `/private/tmp/claude-501/.../scratchpad/wt-t6-3`, branch `t6-3-e2e`, base HEAD `13b83e1`.
- ÖNERİ (karar PM'de): (a) bu bulguları ayrı bir 🔴 bug-fix task'ı olarak plana ekleyip SpringEmitter'ı
  düzelttikten sonra bu testi tekrar koşturmak, ya da (b) T6.3'ü "test eklendi + gerçek regresyon
  bulundu" olarak DONE sayıp bug-fix'i M6 sonrasına ertelemek — SPEC/task sapması olduğundan burada
  karar verilmedi, PM'e bırakıldı.
